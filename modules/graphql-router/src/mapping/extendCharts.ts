export const extendCharts = (config, extendedFields) => {
	/**
	 * TODO: extend with gql types
	 */

	/**
	 * TODO: extend with gql query
	 */

	/**
	 * TODO: validate displayNames match the fields given
	 */

	/**
	 * TODO: validate the chart type supports the GQL field type
	 */
	return config;
};
