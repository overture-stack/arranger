{
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "lerna": "lerna",
    "link": "npm-link-shared modules",
    "publish": "lerna publish",
    "netlify-build":
      "npm i && npm run bootstrap -- --scope @arranger/components && cd modules/components && npm run build-storybook"
  },
  "devDependencies": {
    "lerna": "^2.6.0",
    "npm-link-shared": "^0.5.2",
    "prettier": "^1.9.2"
  },
  "dependencies": {
    "classname": "0.0.0"
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  }
}
