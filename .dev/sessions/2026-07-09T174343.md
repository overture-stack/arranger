Clarified the theming engine roadmap item against the actual code state, ahead of a Tooltip theming step.

- `.dev/roadmap.md`: "Extend the theming engine to all components" corrected to reflect that most Aggs components (`TermAggs`, `BooleanAggs`, `AggsGroup`, `BucketCount`, `RangeAgg`, `DatesAgg`) already participate in the theme engine; remaining gaps narrowed to `Aggregations.jsx`, `AggsQuery.jsx`, `aggComponentsMap.jsx`, `AggsPanel.jsx`, `TermAggs/SelectAllButton.jsx`, and `Tooltip/`; added Tooltip as the named next target (fold its local `TooltipThemeProperties` shape into the central `Components` type, same pattern as Table/Aggregations) with a note that this step does not require the Emotion replacement decision first; flagged the `modules/charts` `ChartsThemeProvider` split from `modules/components`' `ThemeContext` as a known, unscoped gap

Bisected the SQON viewer multi-value bubble regression to its exact origin commit, using two production screenshots (demo.overture.bio) showing the old one-bubble-per-value rendering versus the current broken single-bubble rendering.

- `.dev/tech-debt.md`: "SQON viewer shows multiple values in the same bubble" entry rewritten from "Fix: Unknown; needs investigation" to a confirmed root cause: commit `87b9c1da` (PR #923) changed the value array construction in `modules/components/src/SQONViewer/index.jsx:63` from a flattening `[].concat(...)` to a wrapping `content.value ? [content.value] : []`, which always produces a length-1 array for multi-value filters; this silently breaks `hasMultipleValues`, the `in`/`is` operator label, the enclosing parentheses, and per-value truncation, none of which had test coverage. Fix and regression-test guidance added; no code changed yet.
- `.dev/tech-debt.md`: made rendering tests a required part of the bubble fix, not optional follow-up, since the same regression already shipped once with zero coverage on the code path. Added a new entry, "No rendering-level unit test coverage in `modules/components`; SQONViewer is the natural starting point": surveyed the whole module and found only five test files total, none exercising component rendering; also found a no-op test (`expect(false).toBe(false)`) in the old-style `SQONViewer/__tests__/utils.test.js`, and flagged that file's relocation collides with an existing co-located `utils.test.js` testing a different function from the same source file, so they need merging rather than a straight move.

Ground-truthed four more roadmap items against actual code state (Sets, `displayValues`, Components modernization, Emotion replacement), the same treatment as the theming and SQON viewer items above.

- `.dev/roadmap.md` ("Sets: full feature implementation"): confirmed only one backend operation exists end-to-end (`saveSet`, create-only; no list/delete/update); confirmed the sets ES mapping stores `userId` but nothing enforces it; confirmed `ids` is a frozen creation-time snapshot with the `sqon` stored but never re-run, so virtual cohorts is a new resolution mode, not an extension.
- `.dev/tech-debt.md`: updated the existing `hackyTemporaryEsSetResolution.js` entry to resolve its own open question: `resolveSetsInSqon` is confirmed to run unconditionally, not gated by `enableSets`, so the file cannot be removed even if Sets is "disabled." Added a new entry, "`ENABLE_SETS` flag does not fully gate the Sets query path" (OWASP A01): the flag only gates index creation; the `set_id:` query-resolution path runs regardless, with no ownership check on `userId`, found while verifying the Sets roadmap item.
- `.dev/roadmap.md` ("`displayValues` for all aggregation types"): confirmed accurate; added exact file/line grounding for BooleanAggs' working implementation, TermAggs' TODO-only state, and a previously undocumented gap: the Table's `cells.tsx` cell renderer also doesn't consume `displayValues` even though the same GraphQL-fetched data is already available to it.
- `.dev/roadmap.md` ("Components module modernization"): added confirmed grep counts: `recompose` in 4 files, `component-component` in 10 (7 of which are in `AdvancedSqonBuilder/` alone), 21 class components spanning both legacy code and the already-modernized theme engine and Aggs family; named `AdvancedSqonBuilder` as the natural first target.
- `.dev/roadmap.md` ("Replace Emotion with a less constrained styling solution"): added confirmed footprint (46 of the module's files import `@emotion/*`, 39 use the `css` prop, 6 use `styled(...)`) to make explicit that this is a module-wide migration, not a contained one.

Migrated the agentics upstream-adoption diagnosis's first flagged gap: this project had never adopted the current agentics session-file model.

- `.dev/sessions.md` (single shared file, 23 dated entries) split into `.dev/sessions/`, one file per day (`YYYY-MM-DDTHHMMSS.md`); historical entries kept verbatim (date headers and `---` separators dropped, since the filename now carries the date), split by date only per the migration procedure, no retroactive contributor attribution
- `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`, `DEVELOPMENT.md`: every reference to `.dev/sessions.md` updated to point at `.dev/sessions/` and describe the new per-day-file lookup (list sorted by filename, read the most recent 1-2)

Closed the remaining four diagnosis gaps together: brought all three agent instruction files' "Starting a session" and session-discipline content to full parity with each other and with the current agentics conventions, inlining the substance instead of pointing at it.

- `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`: "Starting a session" now opens with an explicit list of session-start-signal phrases (greetings, resumption, explicit, on-demand) and leads with an instruction-file integrity check (`git log --oneline -1` on all three files) that was previously present only in `CLAUDE.md`; all three now end with the same staleness-pass reminder. `.github/copilot-instructions.md`'s prior unordered "Key files to read" became a proper numbered checklist matching the other two, gaining the integrity check and the `/docs`-is-stale reminder it previously lacked entirely.
- `CLAUDE.md`, `AGENTS.md`, `.github/copilot-instructions.md`: session-discipline sections now inline (rather than only link to) the per-contributor-per-day session file model, clarified for this repo's actual shape (one human contributor across multiple AI tools, so "per day" in practice), and the verify-against-current-code-before-marking-anything-done rule.
- No content was force-matched to the agentics template where Arranger has a working local deviation: the tech-debt entry format (richer than the template's minimal version) was left untouched, since the diagnosis procedure itself says local deviations should not be silently overwritten.

Iterated the `build_sqon` design doc: fixed a real design flaw (the LLM was still being asked to author a SQON-leaf-shaped object), documented internal quirks a v3 implementer needs to know, and evaluated two ideas that didn't survive scrutiny.

- `.dev/docs/build-sqon-tool.md`: flattened `build_sqon`'s parameters, the earlier `filter: { fieldName, operator, value }` nested argument was structurally a SQON leaf's `content` with the `op`/`content` wrapper stripped off, reintroducing exactly the hand-authored-shape risk the tool exists to remove. Now `fieldName`/`fieldNames`/`operator`/`value` sit flat on the call, siblings of `combination`/`negate`/`existing_sqon`; updated every worked example, the Zod sketch, and the handler steps accordingly.
- `.dev/docs/build-sqon-tool.md`: added "Known quirks to account for in v3 design", `reduceSqon`'s auto-merge behaviour is combinator-dependent (e.g. two `gt` clauses collapse to the max under `and`, the min under `or`; `between` never merges), single-item groups auto-unwrap unless pivoted, `not` groups never flatten into an outer group, group combinators are limited to `and`/`or`/`not`, and every node schema uses Zod `.passthrough()` so stray extra keys pass validation uncaught.
- `.dev/docs/build-sqon-tool.md`: added "Relationship to `execute-query`'s current raw-SQON path", the shipped cheat-sheet approach predates this design and was the right interim step; once `build_sqon` exists, `execute-query`'s required tool-call sequence needs to change and the cheat sheet's role narrows, framed as expected follow-on work, not a correction of the existing implementation.
- Considered and rejected a compact-string `filter` param (e.g. `"in 'status': ['active']"`) and TOON as `build_sqon`'s input format: both would make the LLM author a hand-written grammar with no schema backstop, reintroducing the exact risk class the flattening fix just removed. TOON is only a good fit for tool *outputs* the LLM reads, not inputs it authors, per the already-deferred roadmap note.
- Considered and deferred batching multiple clauses into one `build_sqon` call: documented in the doc as deferred rather than built, since one-clause-per-call keeps error handling atomic (a bad clause never disturbs what's already accumulated) and batching's real justification (avoiding the `existing_sqon` re-echo cost) only gets strong once v3's nested groups exist.
- `.dev/docs/build-sqon-tool.md`: alphabetized every named-property example (JSON snippets, all three worked `build_sqon(...)` calls, the full call signature, the Zod schema sketch) per the project's existing property-ordering convention, applied to docs and pseudo-code, not just real config files.

Reviewed PR #1077 (`execute-query` MCP tool) before it squash-merged into `main`, then resolved the resulting conflicts and re-applied the conventions review to what landed.

- Found the PR's branch, though freshly synced onto current `main`, still added a tech-debt entry for the `initializeSets` startup race already fixed and marked `_Resolved._` in `roadmap.md` on 2026-06-30; drafted (not posted) three review-thread replies, closing that stale thread, confirming the dangling-`$ref` tech-debt item is still genuinely open, and pointing the Zod-schema-drift thread at this project's own `build_sqon` design.
- Drafted a clean squash-merge commit message distilling the actual contribution (the `execute-query` tool, its query builder/validator/results helpers, hardened error handling, three new tech-debt items) rather than the default concatenation of in-PR "Merge branch 'main'" and cleanup commits.
- Diagnosed a `git status` showing 7 unmerged files as `stash@{0}`, a 3.5-week-old Tower auto-stash from 2026-06-17, applied against current `main` by an accidental keyboard shortcut; every hunk in every file showed `main` as a strict superset of the stash's content, so resolved by taking `main` throughout, completing the `sessions.md` deletion in favour of `.dev/sessions/`, and dropping the now-redundant stash.
- After the squash-merge landed (bringing the stale `initializeSets` entry in anyway, despite the review-thread ask), resolved the resulting `.dev/sessions.md` modify/delete conflict by porting PR #1077's real session-log contributions into the per-day structure (new `2026-06-10T000000.md`, `2026-06-11T000000.md`; extended `2026-06-16T000000.md`, `2026-06-24T000000.md`), and removed the stale `initializeSets` tech-debt entry now that it had actually landed.

Reviewed dash, name, and spelling conventions across everything touched this session, on request, "through and through."

- `.dev/roadmap.md`, `.dev/tech-debt.md`, `.dev/sessions/`, `.dev/docs/build-sqon-tool.md`: fixed em dashes and space-hyphen-space connectors (colons/semicolons/parentheses instead) in both newly-merged content and this session's own recently-written prose; depersonalized three references to individuals ("Rakesh's MCP implementation", "for Rakesh and Mitchell", "confirmed by Andy") to describe the work instead; fixed `stabilises`→`stabilizes` and two `behavior(al)`→`behaviour(al)` instances.
- Caught and fixed my own error mid-review: wrote "Analysed"/"analysed" in the newly-authored `2026-06-11` file on the belief Canadian follows British `-yse` for this word family (per the then-current global rule). The developer flagged it against [a cited Canadian-spelling reference](https://pressbooks.library.torontomu.ca/authorsguide/chapter/canadian-spellings/): Canadian actually uses `-yze` here (analyze, paralyze, catalyze), no divergence from American for this suffix, unlike the real `-ise`/`-ize` split. Fixed both instances.
- `~/.claude/CLAUDE.md` and, on explicit instruction, `agentics/template/CLAUDE.softeng.md`, `agentics/template/AGENTS.md`, `agentics/template/conventions/code-style.md`: corrected the `-yse`/`-yze` rule itself, it had the divergence backwards. Added a `CHANGELOG.md` entry in agentics (`template`, non-breaking) documenting the correction and its source, following the existing precedent for personal-file-plus-repo fixes of this kind.
- `.dev/roadmap.md`: added "Update `/docs` for the MCP server surface" as a fifth subitem under "MCP integration readiness", the recurring `/docs`-is-stale open thread never promoted to a tracked item; nested the `docs/concepts.md` `fieldName`/`fieldNames` definition check (it currently ties both names to sitting "within a filter clause's `content` object", which `build_sqon`'s flat arguments will make narrower than reality) under it as a to-verify item once `build_sqon` ships, rather than editing concepts.md now for a tool that isn't built yet.
