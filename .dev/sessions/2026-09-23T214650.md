Planned how filtering, access control and the router's vocabulary separate, so that Arranger keeps running unchanged with no Usher at all. Recorded the plan without making any of the moves.

- `.dev/docs/atlas/layers-and-vocabulary.md`, `.dev/docs/atlas/index.md`, `.dev/docs/arranger-auth/index.md`: new reference for which layer owns what and what each calls the filter, indexed and linked from the auth index so it is found from outside the Usher work too. `accessControl/` holds nothing that depends on Usher, which is why it becomes `filtering/` and access control is reserved for `usher-adapter`.
- `.dev/docs/arranger-auth/roadmap.md`, `.dev/docs/arranger-auth/usher-adapter.md`, `.dev/docs/arranger-auth/auth-mode-changes.md`: Phase 1 gains a requirement that reduction preserve a filter's meaning when a group is empty, due before the Usher adapter builds constraints with `SqonBuilder`. Later items renumbered, and the four citations of them updated.
- `.dev/docs/arranger-auth/`, `.dev/docs/atlas/`, `.dev/docs/components-multicatalogue.md`, `.dev/roadmap.md`, `.dev/tech-debt.md`: the Usher sense of "plugin" is now "Usher adapter", qualified every time because "adapter" alone already names the Beacon and REST transports. Other plugins (GraphQL, eslint, OpenSearch security) keep the word. `usher-plugin.md` is now `usher-adapter.md`, which also repairs four references still pointing at its location from before it moved into `arranger-auth/`.
- `.dev/docs/arranger-auth/usher-adapter.md`: configuration shaping recorded. Six surfaces with no shared gate, the per-request hook the configs resolver never declares, `meta.authFiltered` as the omission signal, and GraphQL introspection as the one surface shaping cannot reach, since the schema is built once per catalogue. The design ask for configuration that can hide a component per principal is recorded as open.
- `.dev/docs/atlas/layers-and-vocabulary.md`: the bridge mounts above both of `arrangerRoutes.ts`'s catalogue branches, since a single catalogue mounts with no prefix and several under `/<catalogueId>`.
- `.dev/docs/arranger-auth/roadmap.md`: Phase 1 item 4 names where `getServerSideFilter`'s type is optional below the routing boundary. Every call site fails closed, so the first step is a type change.
- `.dev/roadmap.md`, `.dev/tech-debt.md`: the mapping-drift detector also needs to run on mapping change, since Arranger derives state from the mapping once at load. The cardinality check is a bounded, report-only sample at catalogue load, not a scheduled one.
- `.dev/tech-debt.md`, `.dev/docs/atlas/roadmap/structured-logging.md`: the introspection response spells the catalogue id both ways depending on whether the catalogue failed, and the planned logging field `catalogId` has its spelling owed before it ships.
- `modules/graphql-router/src/mapping/utils/compileFilter.js`: TODO marking the parameter and TSDoc renames owed when filtering separates from the GraphQL layer. The error messages keep the router's names deliberately, since their reader can only act through `getServerSideFilter`.

Recorded a published-contract change to the SQON JSON Schema that had no changelog entry.

- `CHANGELOG.md`: the schema's `$id` move to `overture.bio`, without the `arranger` segment, is now a breaking-change entry, since anything keying on the `$id` sees it change.
