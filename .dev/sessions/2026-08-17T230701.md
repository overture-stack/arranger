Ran a second full-repo review on request, deliberately at different angles from the same day's first pass (which had reviewed each package's source in isolation). Read `.dev/roadmap.md` and `.dev/tech-debt.md` in full first, per the request, to avoid duplication and to use them as review targets in their own right. Eight parallel read-only agents covered: cross-document link and citation integrity, repo-level infrastructure, dependency health, cross-workspace config drift, the roadmap as an artifact, error and logging conventions systemically, the public API surface as an integrator sees it, and test strategy rather than per-file coverage.

- `.dev/docs/atlas/monorepo-review-2026-08-17-second-pass.md` (new, indexed): triage entry point for roughly 90 findings, plus the cross-cutting patterns and three reference tables that existed nowhere in the repo before (the `npm audit` baseline, the testing-layer map, the public-export inventory). Leads with the single most significant result: seven independently-found defects all converging on the `/download` route, found by four lenses that did not know about each other. The route has no tests at any layer, its formatter's only tests are skipped in a way Node reports as `skipped 0`, `disableDownloads` is never checked so the route mounts unconditionally, `disableFilters` does not cover it, a `date` column holding an unparseable value throws `ReferenceError` and kills the whole export, an unauthenticated `mock` flag corrupts pagination, and TSV output has no formula-injection or delimiter escaping. Recorded as one finding about untested code rather than seven.
- `.dev/tech-debt.md`: added verified high-severity entries across six sections. `tsconfig.eslint.json` says `"extend"` instead of `"extends"`, confirmed via `tsc --showConfig`, so every type-aware ESLint rule for all 10 workspaces has been running at `target: ES5` with `strict` off. A dead 2018 Storybook install in `modules/components` (no script, zero stories, `.storybook/` last touched 2019-12-24) is the source of 42 of the repo's 45 `npm audit` criticals. `modules/components`' Jest collects only `dist/`, so its 59 tests assert against the last build and any test added to `src/` is silently never run. `AggsWrapper` is exported from the public entrypoint and does not exist. `@overture-stack/arranger-types`' declarations promise 29 symbols its ESM build drops, verified end to end: the import typechecks clean and is `undefined` at runtime. Nine unused runtime dependencies in published `arranger-components`, three of which are its production security findings. Plus the AGPL-versus-undeclared-license gap across all eight manifests.
- `.dev/roadmap.md`: corrected six stale or wrong items found by reviewing the document as an artifact. Three broken anchors and one cross-repo path fixed mechanically. The §3.1 Changesets cleanup note still asserted a mechanism §3.3 had already recorded as false and moved the `fix-workspace-deps.mjs` removal to the phase that actually owns it. The Changesets ignore list named `integration-tests-server`, which is not a real package (`integration-tests-search-server` is), and survived an audit of that same list earlier the same day because auditing a list is not verifying its entries. The OpenSearch item claimed a missing client dependency that has been present all along. The Storybook item was scoped as greenfield when charts already has a complete empty Storybook 9 harness and components has a dead Storybook 3 one. The dependency-graph reference was wrong in four ways that all understated what a `sqon` change rebuilds.
- `.dev/roadmap.md` §3.3: recorded a confirmed blocker for the next planned migration. `modules/charts` declares `@overture-stack/arranger-components` peer-only with no dev or prod counterpart while seven source files import it, resolving today only via npm's workspace-root symlink, so pnpm's strict isolation breaks it on first install. More consequential than the fix: this is the second confirmed `depcheck` blind spot after `ts-patch`, meaning the phantom-dependency audit's "clean" verdict for the remaining packages rests on weaker evidence than it reads as, and should be re-run by a method that does not share depcheck's assumptions.
- Repo-infrastructure findings (the one lens whose agent had to be rerun after an infrastructure failure) added three things nothing in `.dev/` previously knew about, none of them source-code defects. `make start` has been broken: `docker-compose.yml` targets a `server` build stage that `Dockerfile.local` does not define (its stages are `scaffolding`/`search-server`/`mcp-server`), and its config volume mounts to a path the server does not read while interpolating `CONFIG_PATH` where the app and docs use `CONFIGS_PATH`, so fixing the stage name alone still yields zero catalogues. `CHANGELOG.md` had explicitly told operators to update this file during the image rename; the Dockerfile and Jenkins pipeline were updated and the compose file was not, and CI never exercises compose, so the only broken path is the one a new contributor takes first. There is no security disclosure channel at all (private vulnerability reporting disabled, no `SECURITY.md` here or in the org defaults) while `CONTRIBUTING.md` routes bug reports to a public forum. And the licensing gap is confirmed as an oversight rather than a position: sibling Overture repos declare AGPL in their manifests, and `@overture-stack/sqon` declares nothing while the `sqon-builder` it replaces declares `AGPL-3.0-or-later`, so migrating on the project's own advice is a compliance regression.
- `.github/copilot-instructions.md` was deleted by the developer, deliberately, applying agentics' `copilot-instructions-retire-not-sync` decision (2026-08-17, currently unreleased there): GitHub Copilot's coding agent, CLI, and VS Code Chat all read `AGENTS.md` directly now, so a second instruction file only creates drift, and this project's own copy had already drifted. Initially raised as a governance concern before that context was known, which was the wrong read. Completed the cleanup the deletion left behind: removed the file from `DEVELOPMENT.md`'s AI-tools list (with a short note recording why it is gone), dropped it from `AGENTS.md`'s session-start instruction-file integrity check, which had been silently passing on a path it could not see, and dropped it from `AGENTS.md`'s self-edit prohibition list.
- Applied eight fixes from the review, each picked for being unambiguously correct and verifiable immediately, and each verified rather than assumed: the `dataToExportFormat` `ReferenceError` that killed exports on unparseable dates (rebuilt and re-ran the repro); the three `docker-compose.yml` defects breaking `make start` (`docker compose config` validates); the `tsconfig.eslint.json` `extend`/`extends` typo (`tsc --showConfig` now shows the base inherited); `modules/charts`' `.dist/` main-field typo; the phantom `AggsWrapper` export in `modules/components` (rebuilt, absent from both `dist/index.js` and `dist/index.d.ts`); the missing `imports` field in `modules/types` (alias resolves under `tsx`, 29 tests pass and `sqon`'s 125 unaffected); two broken references in `CHANGELOG.md`; and repo-level `.gitignore` entries for `.claude/`, which had been excluded only by a machine-local rule. Left deliberately unfixed and recorded as such: the license fields (need the client-side AGPL question answered), the `charts` peer dependency and the Storybook 3 removal (both need a lockfile regeneration, not a manifest edit), and the test-configuration fixes (they would surface previously-unrun tests, which is work rather than a fix).
- Verifying one of those fixes surfaced a second, quieter bug in the same function that reading it had not: `dateHandler` renders dates a day early on negative UTC offsets, because `new Date('2020-01-01')` parses as UTC midnight while `date-fns` formats in local time, so a valid `'2020-01-01'` emits `2019-12-31`. Every date in every export is shifted for North American deployments. Logged rather than fixed, since choosing between local-parsing and UTC-formatting is a semantics decision, most likely resolved by reordering the `switch` so `parseISO` (which handles date-only input as local) precedes the `new Date` branch.
- Verified the highest-stakes claims directly rather than transcribing them. Three times a quick check produced a false negative that made a correct finding look wrong (a zsh glob aborting an `ls` and hiding the root `LICENSE`, a test harness using the wrong gate property so `dateHandler` was never reached, and grepping a `.d.ts` for a name that only appears via `export *`). All three held up under a proper test; recorded in the atlas doc as a calibration note, since the pattern was consistent enough to be worth naming.
- Agentics upstream check (mandatory per global context, and prompted directly this session): this project is stamped `0.13.0 | synced: 941106cb`, while agentics has released `0.14.0` and carries further unreleased work on top. So Arranger is one released minor behind plus the unreleased set, of which the copilot retirement above is one item already applied by hand. Surfaced rather than actioned: a full 0.13.0 to 0.14.0 upgrade pass is its own scoped task, and `AGENTS.md` is developer-owned.
- Agentics upgrade applied on the developer's decision, 0.13.0 to 0.15.0 (`synced: 988db215`), working from the real template rather than a relayed summary. All nine non-conflicting items: the check-in bullet's machine-state disclosure clause, the "How to resolve these paths" block, the `CLAUDE.roles`/`CLAUDE.softeng.md`/`CLAUDE.overture.md` renames to their `AGENTS.*` equivalents, `docs/agent-security.md` added to both the PR-review and security dispatch rows, three new dispatch rows (`upstream-check.md`, `context-economy.md`, `agent-index.md`), the memory-hygiene example trim, and the initialization section rewritten to defer to the template's canonical step list while recording every already-answered question so none recurs. Verified every dispatch path resolves against the real agentics template before finishing. On the conflicting item, the developer chose to adopt upstream, so the local "No commits" line is removed and the rule now comes from `session-discipline.md`'s pointer.
- Two additions beyond the batch. The new path-resolution block collides with this project's own `docs/` directory: it declares `docs/` paths as resolving into agentics, but this repo references its own `docs/concepts.md` for domain vocabulary. Narrowed the exception to name `docs/agent-security.md` specifically and added a paragraph distinguishing the two, since an agent following the block as written would look for Arranger's concepts doc inside agentics. Worth propagating upstream: any adopter with its own `docs/` tree hits this. Separately, corrected the stale Structure block (added `modules/charts` and `apps/mcp-server`, replaced the nonexistent `integration-tests/admin` entry with `mcp-server`, and noted both admin remnants are inactive), which closes a finding from this session's review.
- `roadmap_split: yes` applied, recorded in project memory rather than global context since it is a per-project choice. Four entries dense enough to need it (MCP integration readiness, GA4GH Beacon v2, multicatalogue lifecycle, Changesets adoption) are now terse summaries cross-linked to detail files under `.dev/docs/atlas/roadmap/`, indexed in the atlas index; `.dev/roadmap.md` went from 1181 to 983 lines with 244 lines moved verbatim, and every cross-link, back-link, and internal anchor verified. Entries under roughly 30 lines were left dense on the stated basis that the convention splits entries "that need it" rather than all of them. **That was wrong**, corrected the same day after a peer challenge. The phrase came from `template/AGENTS.md`'s initialization question, not from `session-discipline.md`, which is the governing rule and which already said (in 0.15.0, the version synced here) to sweep the file and relocate *each* entry's justification, alternatives, or history, calling partial application something that "solves nothing." The correct test is per-entry and about content, not about how dense an entry looks. Measured the real remaining scope: of 73 entries, 5 are routed to a detail file, 47 are already conformant (what plus status only), and **21 still carry reasoning and were skipped**. Left in that state pending a decision on completing the sweep versus reverting to unsplit, since the convention rules out leaving it partial. Separately, `4.4 Renovate` was briefly split by mistake (a line-count bug ran the entry to end-of-file) and reverted once its real 3-line size was confirmed.
- Investigated whether the `buildAggregations` `startsWith(nestedPaths)` array-coercion bug was safely fixable now, ahead of the Usher work. It is not, and the empirical result corrected this session's own earlier framing of it. Confirmed the coercion is depth-dependent: harmless at depth 0 (the computed filters are never used, since `isNested` is falsy), correct by coincidence at depth 1 (a single-element array stringifies to exactly its element), and dead at depth 2+. But the earlier entry's claim that this drops nested-filter exclusion, implying wrong counts, was wrong: `injectNestedFiltersToAggs` independently applies the same filters via a `<path>:filtered` wrapper and works correctly at depth 2+, so counts are right today and the expression is simply dead where it would matter. Applied the obvious `.some()` fix to test the premise: the suite went 10/10 to 7/10, failing exactly the three depth-2 sibling-filter tests, and dumping the real output showed both wrappers firing nested inside each other. The two mechanisms do not share boolean semantics, `injectNestedFiltersToAggs` builds `bool.should` while `createFieldAggregation`'s `:nested_filtered` builds `bool.must`, so the fix silently changes multi-filter depth-2 aggregations from OR to AND-on-top-of-OR. Equivalent for one sibling filter, which is why every existing fixture passes for the wrong reason. Reverted cleanly, suite back to 10/10.
- Rewrote the tech-debt entry accordingly: corrected severity and framing, recorded the empirical evidence rather than the caution, and marked it URGENT specifically for the Usher/ABAC work. The reasoning: the usher-arranger plugin's whole guarantee is that a grant-derived filter reaches both records and aggregate counts, and that runs straight through this code. Two overlapping mechanisms with different boolean semantics, one dead at the nesting depth real clinical schemas use, is not a foundation for access control, since an access filter that becomes OR where AND was intended is an over-disclosure. Needs a design decision (which mechanism owns depth-2 nested filters, and whether `should` or `must` is intended) plus a two-sibling-filter fixture, which no current test has.
- Answered the Usher session's design brief with Arranger-side constraints, engaging as though the aggregations issue were resolved, per the developer's instruction. Key input given: `arrangerRouter()` is already invoked once per catalogue, so a router-level plugin is catalogue-scoped by construction, which turns their fieldName-collision guarantee from a request-time check into a structural property if per-catalogue plugin instances are built at startup rather than one plugin holding a merged map. Also distinguished the two denials they had conflated (catalogue-absent belongs above both pipelines as a 404 matching the existing failed-catalogue body, never as an empty filter, which returns 200 with zero buckets and discloses existence), corrected their assumption that aggregations run before server-side filters, flagged that `buildQuery` and `buildAggregations` are separate implementations so a correctness argument for records does not transfer to counts, and raised `nestingPrefix` double-prefixing as a risk for their environmental fieldName choice. Asked whether Usher's grants payload can express "open access, no filter needed" distinctly from "absent, deny", since the environmental catalogue may be the former.
- Completed the `roadmap_split` migration on the developer's decision, this time conformantly. Applied the convention's per-entry content test to every entry rather than ranking by density: of 73 entries, 24 had their justification, alternatives, and history extracted, 2 resolved items (`nestingPrefix`, the `initializeSets` startup race) were removed outright per the working-docs rule that done items leave the roadmap, and the remainder were already conformant (what plus status only). `4.4 Renovate` is the clearest example of the latter, a single what-and-status line that the earlier density heuristic had flagged only because of a line-counting bug. Followed the extract-verbatim-then-rewrite sequence as two separate steps, which is now an agentics rule and originated from this project's earlier pass. Grouped into nine topic files rather than one per entry so cohesive areas read as one document; `nestingPrefix`'s implementation record was appended to the existing `.dev/docs/nesting-prefix.md` rather than duplicated into a new file. `.dev/roadmap.md` went 1181 to 819 lines with 594 lines now under `.dev/docs/atlas/roadmap/` across 13 files. Verified afterwards by re-running the conformance test (zero entries still carrying reasoning) and checking every cross-link, sub-anchor, and internal anchor resolves.
- Agentics accepted the `AGENTS.md:68` correction and fixed it in three places, including removing the qualifier from the initialization question itself. They also named the shared shape between that error and the credential-blocklist bug shipped earlier the same day: a scoped check reported as an unscoped finding, which is most dangerous for absence claims because a scoped search returns clean and clean reads as proof. That is now a general rule in `review-conduct.md`. On the sampling-bias question raised unprompted, they checked rather than accepting it and found the counted evidence does not support "three consecutive releases dominated by one project" (1 of 29, 0 of 8, 2 of 25 entries naming this repo); they logged the deeper problem, that the anonymization keeping project names out of released text is what makes the question unanswerable by grep, as an open design question.
- Created `.dev/docs/arranger-auth/` as the home for the access-control subsystem, replacing the single `.dev/docs/usher-plugin.md` (moved into the directory, not duplicated). Four files: `index.md` (entry point and reading order), `design.md` (the enforcement-seam decision and its reasoning), `roadmap.md` (sequencing), `debt.md` (scoped index of access-control-path defects). Deliberate structural choice, flagged to the developer rather than assumed: the subsystem's `roadmap.md` and `debt.md` are scoped *views* that order and annotate work while cross-linking canonical entries, not second stores. A subsystem-local copy of either would be invisible to the session-start checklist, which reads `.dev/roadmap.md` and `.dev/tech-debt.md`, and would drift, which is the failure mode this session spent most of its time finding instances of.
- The design decision recorded: enforcement belongs at the query-building boundary, not the transport boundary, so the Usher plugin is a translator only (GrantsPayload to SQON) while Arranger owns a seam guaranteeing any additional filter reaches every read path. The decisive argument is Beacon, already planned as a non-GraphQL transport with record-level granularity gated on Usher: if enforcement is Express middleware on `graphql-router`, Beacon inherits nothing and needs a second implementation, and this repo already carries five independent copies of the GraphQL-name transform to show where that leads. Also recorded: a separate `arranger-auth` module is premature, since enforcement must sit on the code path that builds the query, which lives in `graphql-router` today and moves into `arranger-core` later, so creating a module now means moving it twice.
- Found and logged a live access-control bypass while verifying the seam, prompted by the developer asking whether the per-catalogue filter wiring was search-server-specific. `getServerSideFilter` is composed into three of the four read paths (`resolveHits.js:271`, `resolveAggregations.ts:99`, `resolveSets.js:79`) and **not** into the export path: `getAllData.js:51-53` calls `buildQuery({ filters: sqon })` with the client SQON raw, and the callback is never invoked anywhere under `download/`. So any deployment using server-side filters for record-level access control can have it bypassed by exporting rather than querying, today, with no plugin involved. Compounding: `disableFilters` also misses `/download`, so the route bypasses both mechanisms, and `/download` has no tests at any layer. Logged as a canonical high-severity `tech-debt.md` entry, and it is Phase 0 of the auth sequencing rather than Usher work, since it needs fixing regardless.
- Also established, correcting an assumption worth recording: per-catalogue filter scoping is **not** `apps/search-server`'s doing. `arrangerRouter()` is a single-catalogue unit taking one `catalogueId` and one filter callback, so any caller including a custom Express server gets per-catalogue scoping by construction. What search-server adds is orchestration (failure isolation, aggregate status, `/:catalogueId` mounting, `documentType` aliasing). The caveat that matters for access control: the guarantee is a property of how `arrangerRouter` is called, not something it enforces, so a custom server can miswire it silently, and the export bypass is that same class of failure already realised in this repo's own code.
- Ran a five-lens Phase 0 sweep for silent enforcement gaps, documented in `.dev/docs/arranger-auth/phase-0-audit.md` (27 numbered items) with canonical entries added to `.dev/tech-debt.md` as each lens reported, per the developer's instruction to write findings down as they came rather than accumulate them. The sweep's own criteria were made explicit first: on the enforcement seam, exists independent of Usher, and fails silently, with the last condition load-bearing since it is what makes downstream enforcement appear to work. Lenses were told to prefer executing code over reading it, on the evidence that two of the three previously-known items were found by execution and missed by reading.
- The result substantially changes the picture. Of Arranger's four mechanisms for restricting what leaves it, three can be defeated by an ordinary GraphQL client and the fourth (export) was already known. Highest-severity findings, each verified independently after the reporting agent: an empty grant set compiles to match-all, so "this user is entitled to nothing" grants full-index access for three of four natural encodings; aggregations on a server-side-filtered field escape via an ES `global` wrapper returning whole-index counts, with `top_hits` returning whole documents through the same hole, making the aggregation surface a record-disclosure channel; federated queries never send the filter to remote nodes, confirmed by zero `getServerSideFilter` references anywhere under `network/` against 38 elsewhere; `disableFilters` is bypassable three ways (inline literal, renamed variable, or a host app with no body parser); `sets` is a root-level searchable catalogue returning other users' set contents with no ownership check; and the README's documented `getServerSideFilter` example uses `field` where SQON requires `fieldName`, compiling to `{"bool":{"must":[null]}}`, so an operator who followed the only public documentation of the only access-control mechanism has a filter that restricts nothing.
- Three lenses independently converged on the `disableFilters` bypass and two on the `global` escape, from different starting questions and without shared context, which is stronger evidence than any single report. The common root cause across the escapes is that filter composition is a convention re-implemented at each call site rather than an invariant enforced at one boundary, which is the same conclusion the enforcement-seam design reached from first principles; the audit is now its empirical backing.
- Rewrote the auth subsystem's Phase 0 accordingly, from three items to six ordered by what a mistake or an attacker reaches first rather than by implementation cost. Recorded one lens claim (that `ENABLE_ADMIN` never reaches a consumer) as needing one more trace step rather than filing it, since `aggregator.ts` does forward an `enableAdmin` and whether it originates from the env read or only from a programmatic override was not settled.
- Rewrote `.dev/docs/arranger-auth/usher-plugin.md` against the audit, which was the one file in that directory whose content predated it. The translation design survived; three of its stated assumptions about Arranger did not, and are now a dedicated "Audit consequences" section: that the filter is ANDed into every query (untrue on three of four paths), that an empty grant set is a benign input (it compiles to match-all, and it is the plugin's most common input), and that a defensively-written callback returning a falsy value is safe (`compileFilter` reads falsy as "no restriction"). Added a fourth consequence the audit raised rather than confirmed: exclusion-shaped filters degrade toward full access under every failure mode found, inclusion-shaped ones toward no access, which reopens a design choice that is not a refactor afterwards. Recorded as an open question rather than decided, since the cost lands on how the grants payload expresses uncategorized records.
- Corrected the plugin's integration point, which had been recorded as "Express middleware, exact hook to be confirmed" and is now confirmed as something else: a `getServerSideFilter` callback factory, one instance per catalogue, built at startup. The middleware framing cannot work because enforcement has to sit at the query-building boundary, and the audit supplied the proof rather than the argument: the callback is in lexical scope at `graphqlRoutes.ts:473` where the network schema is built and is simply not passed, with no type error and no test to reveal it. Two implementation constraints follow and are now written down: the callback must be total and cannot await, so bridge calls belong in an upstream step that populates `context`; and an embedder on the lower-level exports gets unfiltered aggregations and crashing records, which is exactly the wrong way round and is the case the plugin most needs to work in.
- Resolved four of the file's seven open questions against audit evidence (absent-resource handling, multiple catalogues per query, client-side plugin detection, logging), moving them to a "Resolved since this file was written" section rather than deleting them, so the reasoning stays visible. Sharpened the three that remain. Anonymous access got worse rather than better: the pattern the README documents for it is `if (!userId) return null`, and `null` means no filter, so the documented way to handle an anonymous request grants the full dataset, and federation's empty-by-default header passthrough makes every remote request anonymous.
- Added `P0-0` to the auth roadmap, ahead of the six items from the audit rewrite: a test fixture whose server-side filter actually restricts something. Every existing test in the repo passes `() => null` or `() => undefined` as `getServerSideFilter`, so nothing anywhere exercises a filter that filters, which is the direct reason three of the four escape routes survived review. It is also the mock grants payload the plugin needs later, so it belongs in Arranger's own test utilities rather than in the plugin, and it makes the rest of Phase 0 verifiable at all.
- Fixed two index entries that promised an "iMS catalogue field mapping" in `usher-plugin.md` that was never written, in `arranger-auth/index.md` and the subsystem `roadmap.md`. A false pointer in an index is worse than an absent one, since it reads as a place the reader failed to find rather than as work not yet done.
- Relayed the audit outcome to the Usher session, framed as correcting input previously given rather than as commenting on their design, since three of the constraints handed to them earlier this session were wrong. Marked explicitly which claims were verified by execution here and which are inference about their side, and asked for a correction on the one inference that matters: that unauthenticated, grants-not-loaded, and entitled-to-nothing all produce an empty grant set, which is what makes the match-all finding the highest-value item for them.
- The Usher session answered the flagged inference and it was wrong, so the correction was applied across the five places it had already propagated to: `usher-plugin.md` (three sections plus two steps of the translation algorithm), the auth `roadmap.md` P0-a, `phase-0-audit.md` P0-17, `arranger-auth/debt.md`, and the canonical `tech-debt.md` entry, whose heading carried it too. Recorded as a visible correction in each rather than silently edited. The substance: no access is encoded as *absence from the grants map*, handled with a 404 above filter composition, not as an empty grant set; `categories: []` is a specific access level (member access, uncategorized records only) producing a non-empty exclusion; and an unresolved payload is a 503 from the bridge. So all three cases named as the plugin's likely empty-filter inputs were wrong, and the empty-exclusion case that *is* common is the benign one, a fully-entitled user.
- The finding itself is unaffected, since it was verified by execution against Arranger and holds for any `getServerSideFilter` author, but its severity argument is now better. Was: a PEP will routinely emit empty filters. Now: the paths that must deny cannot be permitted to reach filter composition at all, because if one ever does the result is full disclosure rather than an error. That applies equally to a hand-written filter with an early-return branch, and it points at a structural separation rather than at defensive coding in a callback. Two design decisions previously recorded as preferences are now the fail-closed encodings rather than the tidy ones: absent-resource-as-404, and the upstream/callback split.
- Wrote the two-layer contract into `usher-plugin.md` as a table: upstream middleware owns the bridge exchange and resource lookup and short-circuits (503 for no resolved payload, 404 for absent resource), while the callback is a pure synchronous translation of an already-present grant entry. Worth noting this is not merely a preferred arrangement, `getServerSideFilter` is synchronous, so it is the only split the signature permits. Kept "the callback must be total anyway" as defence in depth, on the grounds that the audit's entire result is that this seam converts bugs into silent full disclosure, which is exactly the input class that has to fail closed.
- Sharpened the inclusion-versus-exclusion open question using a negative result the audit had recorded for a different purpose. A filter on a field absent from the ES mapping is emitted verbatim and matches nothing, which is fail-closed for an inclusion filter and fail-*open* for an exclusion filter, since `not(matches nothing)` is match-all. So under the exclusion shape, a sensitive category whose configured field is misspelled, renamed upstream, or absent from one catalogue's mapping silently stops excluding, and the records it stops protecting are the sensitive ones. Recorded a middle option rather than reopening the shape decision: validate every configured category field against the catalogue's live mapping at startup and fail the catalogue closed on mismatch, which is what `nestingPrefix` validation already does and which the audit confirmed fails in the right direction.
- Established and recorded a foundational fact the auth subsystem docs had never stated: **Arranger has no authentication at all, and it is deliberate rather than a gap.** No `jsonwebtoken`, `jwks`, `passport`, `keycloak`, or `openid` in any `package.json`; zero EGO references in source (the sole repo-wide match is `ego-ui` named in a tech-debt note about sibling repos' license fields); the only `Authorization` header Arranger constructs is outbound Basic auth to Elasticsearch. Identity enters through exactly one door, `addContext(patch)` merging arbitrary values into `req.context`, and the hook is `getServerSideFilter: (context: Context) => SqonNode` where `Context` is a generic parameter Arranger never constrains. So Arranger is authorization-only and identity-agnostic by construction. Added to `arranger-auth/design.md` as the subsystem's starting point, since it bounds every other question there.
- Two consequences worth having written down. Arranger is **out of scope for the EGO to Keycloak migration**: nothing to repoint, no token format to change, with the migration surface belonging entirely to the host applications that build `context` (Stage, the iMS portal, OHCRN researcher-ui, bespoke `search-server` deployments). And the upstream middleware layer specified for the Usher plugin is not a bolt-on to Arranger's model but exactly the extension point it already provides, and the only one. Kept a caveat attached in the doc: "no auth" reads as "clean slate" and is not, since the authorization half that does exist is defective in the ways the audit records.
- Process miss worth recording because the rule already existed and was not followed. Asked to contact a session briefed on EGO and Keycloak, identified by a peer as the most recently started one, the right first step per global context (`agent_index: yes`) was to look up Members in the agent index before falling back to `ListAgents` or asking. Instead the peer's guess was sanity-checked only against session naming, found suspicious, and messaged anyway with substance rather than the handshake the index's own Requests section specifies. The guess was wrong. The naming skepticism was doing work the index would have done properly and faster.
- Following the index correctly does not resolve this case either, which may be a real gap rather than a local mistake: the registered Member is `iMicroSeq/submission-service` and **no live session matches that project**, while the developer reportedly set one up within the last few hours. The index says who exists and what they own, `ListAgents` says who is live, and there is no documented step for "the Member you want is registered and not live"; the tie-break rules cover ambiguity between candidates, not absence. Raised with the agentics session as an observation rather than a proposed edit. Did not fan out to the four `virusseq-portal` sessions on another guess, since guessing is what produced the error.
- Closed the one Phase 0 audit item deliberately held back as unverified. The feature-flags lens claimed `ENABLE_ADMIN` never reaches a consumer, which conflicted with `aggregator.ts:17,42` visibly forwarding an `enableAdmin`, so it was recorded as needing one more trace step rather than filed. Traced to completion: the lens was right, and the apparent conflict was **two different config paths sharing a property name**. `localEnvs.ts:84` reads `process.env.ENABLE_ADMIN` and places it at the config root; the aggregator destructures `enableAdmin` from `externalConfigs` only and merges it into `catalogs.fromEnv.enableAdmin`, so the two never meet. `catalogs.fromEnv` does become every catalogue's base config and does reach `arrangerRouter`, which is exactly why the forwarding looked like a counter-example. The root value's only possible consumer is `server.ts:21`, whose destructure omits it.
- Two things make the finding precise rather than a general complaint. Within the same documented server-level group, `enableDebug` and `enableLogs` *are* destructured by `server.ts` and work correctly, so this is specific to `enableAdmin`. And the documentation half of the lens claim is confirmed and backwards: `docs/reference/07-feature-flags.md:44` says the group cannot be set per catalogue in `base.json`, when per-catalogue is the only channel that works, since file JSON merges over `catalogs.fromEnv` and file values win. Filed as a canonical `apps/search-server` entry at medium severity, since it fails closed rather than open. Noted in the fix that `constants.ts:20` already carries a `FIXME` saying the flag must be removed and the facets-versus-numeric-aggs coupling untangled, so removal may be the right answer rather than repair.
- The generalizable lesson, recorded in the audit doc rather than only the debt entry: a property name appearing on a code path that reaches a consumer is not evidence that the value in question travels that path. Holding the item back rather than filing it on the lens's word was correct, and so was not dismissing it on the strength of the same surface observation.
- Prepared seven current-state questions for an upcoming three-way exchange with the iMS submission service and the Usher session, written into `usher-plugin.md` rather than held in context so they survive compaction, ordered by what blocks the most design if unanswered. They are deliberately scoped to facts that cannot be inferred from this repo, since Arranger performs no authentication at all, and the section is marked for deletion once answered.
- Sharpened the first and most consequential question into something answerable without any knowledge of Arranger's internals, which turned a design worry into a checkable three-way choice. The authorization unit has to *be* an indexed, filterable field, because a SQON clause can only reference one; and its nesting depth has a hard threshold, depth 0 is fine, depth 1 works by coincidence (a single-element path array stringifies to exactly its element), and depth 2 or deeper hits the dead `:nested_filtered` expression so filtering falls to `injectNestedFiltersToAggs`, which builds `bool.should`, making it OR where AND was intended once there is more than one sibling filter.
- Grounded that question in this repo's own fixtures rather than leaving it abstract, which materially changed how seriously it reads. Access-control-shaped fields do appear at depth 0 in the integration-test mappings (`acl` keyword, `controlled_access` and `access_denied` booleans), the safe case; but the same file-centric fixture carries `participants.study.data_access_authority` at depth 1, a study-scoped access field inside a nested object, and its nesting runs to depth 3. So an Overture-shaped index can readily place the authorization unit exactly where the defect bites. If iMS answers depth 2 or deeper, the nested-filter reconciliation stops being a Phase 0 correctness item and becomes a hard blocker on the plugin, which changes sequencing rather than adding a task.
- Declined the epistemic half of a "training exercise, lean into the learning role" framing relayed by the Usher session as the developer's intent, while accepting the practical half. Reasoning given: the guidance is right that EGO and Keycloak cannot be inferred from this codebase and must be asked about, but a receptive posture on *Arranger's* half of the exchange would let a wrong claim about Arranger's behaviour get baked into the enforcement design, which is a worse failure than a claim being challenged. Not hypothetical: three of the four Arranger constraints handed to that session earlier in this session were wrong, and execution caught them rather than discussion. Scoping proposed and agreed: receiving on EGO and Keycloak, authoritative on Arranger, mutual correction on the seam. Flagged to the developer separately, since it was a characterization of his intent arriving second-hand rather than from him.
- Added a second, distinct group of eight questions for the same exchange, on the developer's steer that the right posture is inquisitive rather than merely receptive, and that an auth-free application is positioned to raise things the other service may not have asked about its own system. The structural reason, recorded in the doc so the group has a generative principle rather than being a list: in a service that has always had authentication, the auth layer's assumptions are invisible because nothing has ever violated them, whereas Arranger has none and therefore has to make every assumption explicit. Framed as curious rather than as review, with the note that anything already handled is more likely undocumented than unconsidered, since that side's agentics adoption is recent.
- The highest-value one is the disclosure-path inventory, and it carries transferable evidence rather than just a suggestion: Arranger has exactly one access-control mechanism and a deliberate sweep found three bypasses of it plus a fourth already known, so a service with more mechanisms plausibly has more seams rather than fewer. Included the two method notes that made that sweep work, since they are the actually reusable part: reading code was unreliable (two of three previously-known items were found by executing rather than reading), and the sweep only worked once the criterion was "fails *silently*", because a bypass that throws gets noticed and one that returns 200 does not.
- The others target seams rather than mechanisms: failure-path direction (Arranger fails open on an empty permission set, and it took execution to find, because a permissive result is indistinguishable from a correct one); whether read and write permissions are the same thing, which is where a submission-shaped model and a search-shaped model are most likely to disagree without either being wrong; what authorization-relevant value survives the submission-to-index pipeline, which spans two systems and may be nobody's explicit responsibility; service-to-service identity, since a service account with blanket read is a complete bypass rarely counted as part of auth; retroactive permission changes against durable artifacts, noting Arranger's `sets` feature persists document ID lists so a set outlives the grant that produced it; existence disclosure, where both services should draw the line in the same place or a caller can probe one to learn about the other; and whether granting a permission is itself recorded, which is constrained by the other side since Arranger has no structured logging to correlate against yet.
- First substantive exchange with the iMicroSeq submission service, whose answers are recorded in `usher-plugin.md` under "Answers received". They correctly declined two of the three questions rather than guessing: their scope ends at Song, with no Elasticsearch mapping, no indexing logic, and no visibility into what Maestro does on the way to the index, so the mapping and pipeline questions were redirected to Maestro or the Gateway/Pedigree session. What they did answer changed more than expected. The authorization unit at submission is an **organization**, not a study (`hasUserWriteAccess` against `allowedWriteOrganizations`), which partially answers the indexed-field question and sharpens what to ask the indexing side. And their `allowedReadOrganizations` is hardcoded to an empty array, unimplemented rather than unused, so the service is a pure write path.
- Two consequences worth separating. "Does write access imply read access" is a **decision, not a discovery**: no code anywhere answers it, so it belongs in Usher's permissions model rather than in either codebase, and it needs the developer rather than either agent. Usher framed the three candidate shapes well (submitter reads own submissions; organization reads what the organization submitted; study membership grants access to all contributions regardless of submitter), and for consent-constrained data those are materially different, with write access not implying the third.
- The other consequence was the most useful thing this session gave away rather than found. Their empty `allowedReadOrganizations` is semantically "may read nothing" and is the default state of every user until the read side ships, so whoever implements it as a query filter will be writing exactly the code that this repo's audit found compiles to match-all in three of four natural encodings. Sent them the four-way comparison ahead of implementation, on the reasoning that it is far cheaper to get right before the code exists than to find afterwards. They recorded it in their own tech-debt before implementing, and took the narrower note that `verifyEgoJwt` is the test-coverage priority, since an untested token verifier silently turns a bad token into a trusted identity whereas an untested route gate fails loudly.
- The most structural finding of the exchange, and one no single service could see: **the authorization unit is not stable across the pipeline.** Organization at submission, study plus access tier at Song, whatever the transform emits at Maestro, resource plus category include-list in Usher's grants, and a filterable indexed field at the plugin. Five hops, four shapes for one concept. The plugin sits at the far end able to filter only on what reached the index, so if the unit changes identity at any hop the translation breaks *there* rather than at either end. Recorded as a table in `usher-plugin.md`; Usher is adding the same constraint at principle level. It is also the strongest argument yet for startup-validating the category-to-field mapping against the live index mapping, since a per-deployment verification requirement is cheapest to satisfy at boot and that is the only place that catches a pipeline change after the fact.
- Reciprocal finding worth recording because it rhymes with this repo's: the submission service has **zero test coverage of any auth code** (`authMiddleware.ts`, `verifyEgoJwt.ts`, `common/auth.ts`), confirmed rather than inferred. Different mechanism from the null-filter gap here (a write gate rather than a search filter), same root cause: nothing exercises the actual enforcement path on either side.
- Received the submission service's full EGO current-state writeup and analyzed it from the auth-free vantage. Findings that constrain this repo's design are in `arranger-auth/usher-plugin.md` § From their current-state writeup; the architectural convergence it produced is in `arranger-auth/design.md` § Independent confirmation from another codebase, filed there because it is evidence for the enforcement-seam decision rather than plugin detail.
- The reusable lesson, which is about analysis rather than about either codebase: **three facts documented separately in three sections composed into a finding stated nowhere**, that their service has no read authorization at any layer. Each fact reads as a reasonable default in isolation. A document organized by subsystem will not surface that, and neither will a reviewer reading section by section, which is an argument for reading a security writeup twice, once for what each section says and once for what the sections say jointly.
- Applying the session-file discipline argued for in the agentics exchange from this entry onward: state what changed and whether it matters, point at the artifact for the reasoning, and keep current-state descriptions out of an immutable file.
- Asserted that saved-set expansion needed a read-time access re-check, then verified and found it wrong: stored IDs enter as the *client* filter on both paths, so composition ANDs the access filter over them and they can only narrow a result, never widen it. Corrected in `arranger-auth/usher-plugin.md`, and recorded in `arranger-auth/design.md` § A positive case as the one in-repo example of the composition seam working rather than failing. The error is the instructive part: "this stores IDs, therefore it can replay them" is precisely the intuition a seam is meant to falsify, and it was reasoned from rather than checked, on this repo's own code where this session is the authoritative side.
- The developer's correction that today's fully-open iMS data is a production snapshot rather than the target model, with per-submission access levels planned, is recorded in `arranger-auth/usher-plugin.md` § Consequences of the per-submission access model, along with the two constraints it creates: dual-layer enforcement needing a mechanism that keeps two services agreeing (a shared conformance corpus proposed, pending the developer), and mutable access levels creating a reindex-lag window during which this repo serves a stale access decision.
- A collaboration lesson worth keeping for this repo's own review work, from a peer correction received and returned this session. A single message can carry a verifiable claim and an unverifiable one at the same confidence: "the convention says X" was checkable against shared text and held, while "expect most entries to need Y" was a claim about this repo's data and was wrong by a wide margin (47 of 73 entries were already conformant). Acting on the second would have produced exactly the fragmentation the convention exists to prevent, while looking like compliance. The general form: separate what was verified from what is being assumed about the other side's situation, and mark which is which. A wrong prescription that fails visibly is cheap; one that fails while appearing correct is not.
