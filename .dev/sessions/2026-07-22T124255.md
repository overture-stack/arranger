Investigated a Lyric-side sqon-builder migration report, fixed a `docs/` link-hygiene gap, and ran the mandatory agentics upstream-update check (0.2.0 -> 0.8.0).

- An agent working in Lyric reported that migrating off `sqon-builder` onto `@overture-stack/sqon` was a real, harder-than-a-version-bump migration (different operator vocabulary, no builder API). Verified against the actual published package rather than trusting the report: `npm view @overture-stack/sqon dist-tags` shows `latest` still points at `1.0.0-rc.1` (schema-only, pre-builder-absorption), while `rc` points at `1.0.0-rc.2` (published 2026-07-20), which does have the full `SqonBuilder` API. The `latest` tag only moves on a real `release`-branch merge per the Jenkins pipeline's tagging rule, and none has happened since the builder absorption landed. `modules/sqon/docs/sqon-builder-absorption.md` already documents the full migration with a before/after example; it just wasn't linked from anywhere a consuming repo would find it. Added a "Migrating from `sqon-builder`?" section to `modules/sqon/README.md` linking that doc and stating the `latest`-vs-`rc` gap plainly, and a "Cross-repo package migration requests" convention to `AGENTS.md` so a future session points a consuming agent at the package's own docs directly instead of leaving it to infer status from the registry alone.
- Fixed two real relative links in `docs/migration/v3.1.md` (`../../modules/graphql-router/README.md`, `../../CHANGELOG.md`) that pointed outside the `docs/` subtree: since `docs.overture.bio` symlinks in only `docs/`, not the rest of the repo, these 404 on the published site despite resolving fine on GitHub. Converted both to absolute `github.com/overture-stack/arranger/blob/main/...` URLs. Added a "Links in `docs/`" convention to `AGENTS.md` stating the one-directional rule (only outbound links from `docs/` need to be absolute; inbound links into `docs/` from elsewhere in the repo are fine as relative). Confirmed via `symlinker.sh` in the `overture/docs` Docusaurus repo that this same risk applies to every other Overture project's `docs/` folder (song, score, maestro, stage, lectern, lyric); flagged as a candidate for the central `docs.overture.bio` documentation-standards page, not yet acted on there.
- Ran the mandatory agentics upstream-update check. Diagnosed all `breaking: yes` entries since 0.2.0 against Arranger specifically and found none applicable (the wording fixes and bootstrap-artifact rule all target content Arranger never copied locally); scanned `.dev/roadmap.md`/`.dev/tech-debt.md` for leaked names, restated format comments, and process narrative per the newer diagnosis criteria, clean. Restructured `AGENTS.md` onto the current template's model rather than its prior bespoke layout: added an inlined "Interaction parameters" section (previously deferred entirely to the agentics template with nothing local), a proper "When to read what" dispatch table covering every `conventions/*.md` file (previously only testing, session-discipline, and security were reachable, and only via inline links rather than a table), and filled out "Critical constraints" with the template's baseline items Arranger was missing (no credentials/secrets/private URLs in any file, no machine-specific paths or real names, name code not people). Removed the "Structured logging" bullet and the whole "Workflow" section from Conventions: both were full duplicates of content now reachable via the dispatch table (`code-style.md`'s own Structured logging section, and its BDD/library-awareness content respectively), left over from before Arranger's `AGENTS.md` deferred properly. Widened "Config properties: Alphabetize" to cover TS object literals, `type`/`interface` fields, and destructured parameters, not just config files, matching upstream's own widening of the same rule. A same-day 0.8.0 release landed mid-session (session-discipline.md pre-commit tech-debt recheck, "say it once" density rule); both are reachable through the existing live pointer with no local content to fix. Tag updated to `0.8.0 | synced: b1da6962e3e3134bdc7ac194c7cc8705f3849e26`. No `.dev/agentics-overrides.md` entries needed: adopted upstream's dispatch-table model outright rather than keeping a permanent local override.
- Not yet reconciled: `.github/copilot-instructions.md` still inlines BDD/testing details and a shorter Security-triggers list rather than dispatching, which predates this session and wasn't in scope for the agentics check itself (no version tag lives there). Left as a follow-up rather than rewritten unprompted.
- Follow-up sync, `0.8.0` -> `0.9.0`: agentics evaluated this session's report directly (dist-tags and cross-repo-migration-pointer are now generic upstream rules; the `docs/` link rule is now `CLAUDE.overture.md`, a new Overture product-family profile, since it applies to every Overture repo, not just this one). Tag bumped to `0.9.0 | synced: b37cb60ff2668945f74347826ca6ccf24b5e80a9`; added the `Overture project -> read CLAUDE.overture.md at session start` dispatch line. Removed three now-redundant local `AGENTS.md` entries in favour of the live pointers: "Links in `docs/`" (superseded by `CLAUDE.overture.md`), "Config properties: Alphabetize" and "Search before writing" (both already fully covered by `code-style.md`). Trimmed "Cross-repo package migration requests" to keep only the Arranger-specific residue (which packages, which doc), pointing at `documentation.md`/`code-style.md` for the general reasoning instead of restating it. The "Is this an Overture project?" initialization question (new this release) hasn't been asked yet, project memory predates it; will surface next session per `upgrading-adoption.md` § 1's new initialization-question check.

Everything above is unstaged in the working tree; nothing has been committed.
