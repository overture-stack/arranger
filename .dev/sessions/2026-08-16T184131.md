Cleaned up a second duplicate tech-debt.md section, then helped the iMicroSeq portal UI integration (cross-session) trace two real-but-undiscoverable Components hooks for their GA4 analytics work.

- `.dev/tech-debt.md`: removed a second, partial duplicate of the `## modules/sqon` section (its first two entries were exact copies of ones already at the top of the file; the three genuinely new entries, a missing field-type-to-operator endpoint, a duplication risk between two operator-validity implementations, and a dangling `$ref` bug in the published JSON Schema, were relocated into the original section where they belong). Same shape as the `build tooling` duplicate cleaned up 2026-08-15.
- Answered the iMicroSeq portal UI session's questions on `DownloadButton` exporter customization (cross-session, via the agent index Requests board): confirmed `theme.customExporters.function` accepts any callback, not just `'saveTSV'`; confirmed the deep `dist/` import path to the internal `saveTSV` isn't stable and shouldn't be depended on; and relayed their `onExport`-callback proposal as a real API design question for later. When their actual use case needed `saveTSV`'s full column-customizer logic (function-valued `displayFormat`/`displayName` resolved against `allColumnsDict`), provided a faithful, verbatim reproduction of that internal logic rather than a simplified approximation, confirming a real integration now depends on that exact behaviour, not a hypothetical one.
- Traced a second, unrelated question from the same integration: `Aggregations`'s `onValueChange` callback (fires for every facet interaction across all agg types, with the toggled field/value and new SQON) already does what they needed for `facet_filter` analytics, but has no corresponding TypeScript props interface anywhere in `aggregations/types.ts`, so it was genuinely invisible to their type-based discovery. Flagged one real precision caveat before they shipped it: `isActive` reflects the field's overall filter state after the click, not specifically whether the clicked value was added or removed, which matters for their multi-select term facets.
- `.dev/tech-debt.md`: logged the `Aggregations` typing gap as its own entry, cross-referenced with the `DownloadButton` one as two confirmed instances of the same root cause (plain-JS components with real props/callbacks but no corresponding TS interface), tied to the ongoing JS → TS migration rather than treated as two unrelated one-offs.
- Relayed the `DownloadButton` tech-debt entry's two candidate structural fixes for a decision: export `saveTSV`, or add a generic `onExport` callback (the iMicroSeq integration's own proposal). Decided: `onExport`, named to match `Aggregations`'s existing `onValueChange` precedent, keeping `saveTSV`'s internals unexported and free to change later. A `saveCSV` export format was also requested, initially assumed to be a same-logic separator swap; checked the actual server-side code (`modules/graphql-router/src/download/index.js`, `dataToExportFormat.js`) and found no CSV support exists at all, only `json`/`tsv`, with the tab separator hardcoded rather than parameterized, so real CSV value-escaping and a new server-side format case are needed, not a wrapper. Reported the corrected scope before writing anything down. `.dev/roadmap.md` § Features: added both as separate entries (`onExport` higher priority, client-side-only; `saveCSV` lower priority, spans `modules/components` and `modules/graphql-router`), each cross-linked back to the originating tech-debt entry, which now points forward to them instead of reading as still-open maybes.
