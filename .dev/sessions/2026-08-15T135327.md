Continued scoping the pnpm migration: brought up a local Elasticsearch instance to unblock integration-test coverage, then ran a phantom-dependency audit as the first concrete step toward a real effort estimate.

- `DEVELOPMENT.md`: documented the `make start-es` container-name-conflict fix (`docker start elasticsearch.local` instead of letting compose recreate it, since a stopped container from a prior session already held the name), how to verify real cluster health versus Docker's lagging health label, and the confirmed gap that no OpenSearch equivalent exists in the Makefile/`docker-compose.yml` yet.
- `.dev/docs/atlas/pnpm-migration.md`, `.dev/roadmap.md` §3.3: ran `depcheck` against every workspace and recorded the real findings, undeclared-but-imported dependencies in `apps/search-server`, both `integration-tests/*` packages, `modules/components` (three in real source, three in tooling config), and `modules/graphql-router`. Noted the one false-positive category to discount (`#`-prefixed Node.js subpath-import aliases, used repo-wide, that `depcheck` misreads as missing packages). `apps/mcp-server`, `modules/charts`, and `modules/sqon` came back clean.
- Found, by reading `package.json` files directly rather than from `depcheck` (which can't see shell-invoked binaries): `ts-patch` is declared as its own devDependency only in `components`/`graphql-router`; `sqon` and `types` both run `ts-patch install -s` via `prepare` without declaring it, working today only because npm hoists a shared copy. Built a disposable two-package pnpm workspace to test this directly: the `sqon`/`types`-style package failed outright (`ts-patch: command not found`) until the declaration was added, then worked cleanly, confirmed end to end by inspecting the emitted build output for the actual path-alias rewrite, not just a successful install. Resolves the `ts-patch` open question from the atlas doc: `ts-patch` itself is fully pnpm-compatible, the missing declaration in `sqon`/`types` is the only real fix needed. `wireit` was not prototyped, per direct developer confidence that it's not a real risk.
- Fixed all six packages from the phantom-dependency audit: declared the missing dependency in each actual consuming package (`apps/search-server`, both `integration-tests/*` packages, `modules/components`, `modules/graphql-router`, `modules/sqon`, `modules/types`), re-sorting `components`' already out-of-order `dependencies` block while already rewriting it. Verified clean with a second `depcheck` pass and the full test suite. One real catch: `query-string@^9.5.0` (the current, verified-real version) is ESM-only and broke `integration-tests/import`'s test outright, since `components` builds to CommonJS; pinned to `^7.1.3`, the last CJS-compatible major, confirmed by installing it and re-running the test rather than trusting registry metadata alone. Also fixed two pre-existing `"private": "true"` string typos (should be the boolean `true`) noticed in `integration-tests/import` and `integration-tests/server` while already editing those files.
