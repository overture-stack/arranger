Fixed hits and aggregations resolving to `null` for any field whose raw Elasticsearch name GraphQL disallows. Both resolvers sent the sanitized GraphQL name to Elasticsearch instead of the raw path. Verified against a live cluster across single-catalogue and both multicatalogue contexts.

- **The fix is two inverse maps on `graphqlNameRegistry`, because the two call sites need different key spaces.** `_source` selects top-level names, so `rawTopLevelNamesByGraphqlName` covers root fields only: including a raw object path pulls its children, which makes deeper entries unnecessary there. Aggregations flatten a whole dotted path into one name, so `rawPathsByGraphqlFlatName` maps that back to the full path. Putting both on the registry rather than deriving them per call site is what the registry already exists for.
- **`buildAggregations` changes only `esFieldName`, never `fieldName`.** The file already separates the Elasticsearch path from the response key, and the response key has to keep coming from the GraphQL name or `flattenAggregations` and the layer above it would need to learn about raw names. Undoing `__` recovers the nesting but not the characters sanitization replaced, which is why the string transform alone was never enough.
- **That change fixed two things it was not aimed at.** The SQON self-filter comparison and the nested-path lookup in the same function both compare against raw field names, so both were also wrong for awkward names while `esFieldName` carried the sanitized form.
- **The negative control earned its cost, and not by catching a bad fix.** Running the assertions before any resolver changed showed hits returning `null` and aggregations returning a single `__missing__` bucket, which is Elasticsearch reporting that no document carries the requested field: the precise signature of the defect rather than a malformed query. It also exposed a wrong expectation: two fixture records share an `_id`, so three documents index rather than four, and bucket counts written from the record count were off by one. **Fixing first would have meant correcting the test to match its own output**, which is tuning expectations to behaviour rather than deriving them.
- Verified that SQON filtering is genuinely unaffected rather than assuming it. Argument and configuration values carry raw names and are never sanitized, unlike the selection set, so sort clauses, `top_hits` `_source`, and the export path's `columns` are all outside this defect. Existing tests only ever filtered on names that sanitize to themselves, so nothing had actually demonstrated it; a filter on a hyphenated raw name now does.
- **A separate defect surfaced while building the inverse map, and it is not reachable by any encoding.** Two distinct raw paths can claim one aggregation name, and build-time detection covers sanitized leaves within a shared parent, which is a different space from the whole-path one. Logged in `.dev/tech-debt.md` with the concrete case and the reason a different separator cannot help: GraphQL names allow only `[_A-Za-z0-9]`, a subset of what Elasticsearch permits in a field name, so any separator emitted can also occur literally. Escaping schemes either rename every existing field containing an underscore or stay ambiguous where a run of underscores meets a dot.
- Worth keeping the layer distinction that took a while to state clearly: **translating the name for Elasticsearch and choosing the name for the schema are different problems.** The first is unconstrained and a lookup solves it. The second is constrained to the GraphQL alphabet and is lossy by construction, so it can only be detected, never encoded around.
- Coverage gaps left open and logged rather than closed: nested-type fields, the `nestingPrefix` branch, and aggregation types other than `buckets`. A plain object was used deliberately so a nested failure could not be confused with the separate `should`/`must` defect in `injectNestedFiltersToAggs`.

---

Corrected `reduceSqon`'s merge and same-field matching logic in `modules/sqon` across several defects, verified through fuzzing and, for the final state, against a real Elasticsearch cluster (raw vs. reduced query results compared directly).

- Same-field filters now merge only where the underlying set logic is actually correct: `in` merges under `or` only (not `and`, which previously unioned instead of intersecting, widening results it should have narrowed); `not-in`/`some-not-in`/`all` merge under `and` only; range operators (`gt`/`gte`/`lt`/`lte`) merge under `and`/`or` with the correct bound kept. Nothing merges under `not`: `not`'s children are individually negated, so a correct merge there requires flipping the operator itself, which none of these rules do; two same-field clauses under `not` are now left as separate siblings instead.
- `reduceSqon` is idempotent: a second pass never finds more to reduce than the first. A child combination is fully reduced before being folded into its parent, and a merged value array is deduplicated at the point it's produced.
- Same-field matching also respects `pivot`: two leaves scoped to different nested matches (or one pivoted and one not) are never treated as the same filter, in `reduceSqon`'s own merge check, in the exported `checkMatchingFilter` (used by `removeExactFilter`), and in `setFilter` (which could previously replace a pivoted filter with an unpivoted one, discarding its nested scope). `removeFilter` is unchanged by design: it's documented as a bulk, field-scoped removal regardless of pivot, and now says so explicitly.
- `matchNothing()`'s match-none guarantee holds under composition: it can no longer be silently erased by a same-field filter combined with it under `and`.
- `isFieldFilter` no longer throws on a malformed node; returns `false`.
- 154/154 tests pass, build and DTS generation clean.

Not mine, found while verifying the above and handed off: a pre-existing double-negation bug in `graphql-router`'s query compiler (`not[not[x]]` compiles incorrectly), and stale merge-behaviour documentation in the published `docs/reference/03-building-sqon-queries.md`.

---

Restored the value assertions in `integration-tests/mcp-server`'s `execute_query` tests 18-21, which the resolver fix above unblocks. All 84 pass. Then swept the docs for claims the fix made stale.

- **The MCP half of this landed first and was deliberately left half-verified.** `apps/mcp-server` had carried its own copy of the raw-to-GraphQL-name transform applying only the dot-to-`__` rule; it now imports `sanitizeGraphqlFlatName`/`sanitizeGraphqlNameSegment` from `@overture-stack/arranger-types/tools`. Adding catalogue B's awkward fixtures proved the names resolved but exposed the resolver defect underneath, so tests 18-21 shipped asserting names only, with a comment saying why. Restoring the values is the second half of that, and needed no test rewriting beyond deleting the caveat: the assertions were written first and set aside, not derived afterward from what the fix produced.
- **The prediction that entry made was wrong in an instructive way, and I recorded it where the prediction lives** rather than only in the closed entry. The tech-debt entry assumed fixing `apps/mcp-server` would make an awkward field addressable end to end. It did not, because the resolvers were one layer further down. Reasoning about the naming layer alone could not have found that; a fixture with a real hyphenated field did it in one run. Noted in `.dev/docs/build-sqon-implementation.md` under the plan item that called for the entry.
- Stale claims corrected in `.dev/docs/build-sqon-implementation.md`: its "what is affected, and is not this tool's problem to fix" section described `toAggregationFieldName`, which no longer exists; the step 9 manual-test caveat told the reader to avoid hyphenated fields; two rows of the facts table contrasted `sanitizeGraphqlFlatName` against mcp-server's own transform. All four now carry the resolution with its date rather than being deleted, since the reasoning is still the record of why the shape is what it is.
- `docs/mcp-server.md` and `CHANGELOG.md` gained the response-key convention, which is integrator-facing and was undocumented: `execute_query` takes raw names in but returns results keyed by the sanitized ones, while field names inside a `sqon` stay raw in both directions. No "fixed" entry in the CHANGELOG, since 3.1.0 is unreleased and the MCP server is new in it, so nothing shipped broken.
- **One remaining copy of the transform, deliberately not logged:** `serializeToGqlField` in `modules/graphql-router/src/admin/services/index.ts` still applies only the dot rule, and `createNewIndex` writes its output into project metadata. It sits inside `modules/graphql-router/src/admin/`, already logged as a fully disconnected legacy backend slated for deletion, so a new entry would just be a second reason to delete code already marked for deletion. Worth knowing if that subsystem is ever revived rather than removed.
