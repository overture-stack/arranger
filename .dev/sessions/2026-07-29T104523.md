Continued refining the uncommitted partial-availability work and fixed apps/search-server's five pre-existing TypeScript baseline errors, three of which traced to real type-modeling gaps in shared packages rather than local workarounds.

- `modules/graphql-router/src/searchClient/classifyCatalogueFailureReason.ts` (new), `modules/graphql-router/src/index.ts`, `apps/search-server/src/availability/`: catalogue-failure classification moved into `modules/graphql-router` and exported publicly, since only that package has direct knowledge of the ES/OS client error shapes it classifies. `apps/search-server/src/availability/` now holds only the server-wide aggregate status, exposed through a new barrel (`availability/index.ts`).
- `apps/search-server/src/configs/catalogueId.ts`, `introspection/serverDetails.ts`: `ConfigsObject` generic usage fixed to supply its required type argument.
- `apps/search-server/src/configs/fromEnv/localEnvs.ts`: an unrecognised `SEARCH_ENGINE` env value now logs a warning and falls back to auto-detection, instead of being silently trusted as valid via an unchecked type assertion.
- `modules/types/src/configs/index.ts`: `TableConfigs.columns`, `FacetsConfigs.aggs`, `SetsConfigs.index`/`type`, and `ChartConfigs.query` are now optional. None are validated by `arrangerRouter`'s own config validation and all are fully defaulted by its fallback-config merge, so requiring them from callers didn't match actual behaviour.
- `modules/sqon/src/index.ts`, `apps/search-server/src/introspection/types.ts`: `VersionedSqonJsonSchema` (and its component types) now exported from the sqon package root; the SQON introspection response's `schema` field references it directly instead of a duplicated, looser type.
- `modules/graphql-router/src/mapping/extendMapping.ts`: `extendFacets`'s `aggs` now gets the same defensive `?? []` handling `extendColumns` already had for `columns`. Both functions' log messages corrected from "first 5" to "first 10" extended fields, matching what the code has always actually taken.
- `docs/usage/01-arranger-configs.md`: documents that `table.json`'s `columns` and `facets.json`'s `aggregations` are both optional, with the actual fallback behaviour when either is omitted.
- `apps/search-server`: typechecks cleanly; all 5 pre-existing baseline errors resolved.
- `.dev/tech-debt.md`: logged a real bug found while tracing `SetsConfigs` consumers: `modules/graphql-router`'s Sets query filter reads `INDEX` for both the `index` and `type` ES query fields.
- `CHANGELOG.md`: added entries for the config-optionality fix and the sqon type export.

Extended catalogue-failure observability against real production logs from a multicatalogue dev deployment: a permissions error was being misclassified, failure stack traces were always dumped regardless of debug mode, and concurrent catalogue logs used inconsistent identifiers for the same catalogue.

- `modules/graphql-router/src/searchClient/classifyCatalogueFailureReason.ts` (+ test): new `permission_denied` error code, classifying a 401/403 search-client response distinctly from the generic `mapping_fetch_error` bucket.
- `modules/graphql-router/src/router.ts`: a catalogue's full stack trace and cause chain is now only logged to the console when `enableDebug` is set; previously always printed regardless. Extracted the label-precedence logic (`catalogueId`, then `documentType`, then a placeholder) into a named, tested `resolveLabel` function.
- `modules/graphql-router/src/graphqlRoutes.ts`, `apps/search-server/src/arrangerRoutes.ts`: a single `label`/`catalogueId` identifier now threads consistently through the whole startup log chain for one catalogue, replacing four independent per-function fallback derivations that could disagree with each other. New `FALLBACK_LABEL`/`isFallbackLabel` (exported from `graphqlRoutes.ts`) suppress the label clause entirely from log lines when no identifier is available, e.g. a third-party server embedding a single catalogue with no multicatalogue context.
- `modules/graphql-router/src/graphqlRoutes.test.ts` (new), `modules/graphql-router/src/router.test.ts`, `apps/search-server/src/arrangerRoutes.test.ts`: added unit coverage for `isFallbackLabel`, `resolveLabel`, and `catalogueId` actually reaching `buildCatalogueRouterFn`, none of which were tested before; confirmed the last one catches the regression it targets by temporarily reintroducing the old discard bug.
- `docs/usage/05-introspection.md`, `.dev/roadmap.md`, `CHANGELOG.md`: documented the new `permission_denied` code and the debug-gated stack trace behaviour.
- `.dev/tech-debt.md`: logged two pre-existing, unrelated style issues spotted while touching these files: a space-hyphen-space connector in a `graphqlRoutes.ts` log message, and the same pattern used throughout `CHANGELOG.md`'s 33 bullets.
- `apps/search-server/src/introspection/serverDetails.ts`: fixed each catalogue's response object listing `error` before `documentType`, out of alphabetical order; found by inspecting a live `GET /introspection` response.

A catalogue whose GraphQL schema or endpoint failed to build was being silently mounted as `available`, since `arrangerRoutes` (`graphqlRoutes.ts`) swallowed that failure and returned a generic 500-responding handler instead of propagating it; found by inspecting a real production log where a catalogue's schema build threw a GraphQL syntax error but still showed as `available` in the mount summary.

- `modules/graphql-router/src/graphqlRoutes.ts`: `arrangerRoutes` gains an opt-in `rethrowOnError` option (default `false`, preserving today's contract for direct callers of the public `getGraphQLRoutes` export); `router.ts` passes `rethrowOnError: true`, so `apps/search-server` now correctly classifies and reports this failure as `failed` instead of `available`. The three intermediate catch blocks (`getTypesWithMappings`, `createSchemasFromConfigs`, `createEndpoint`) now label their log lines consistently and gate the raw error detail behind `enableDebug`, resolving a `// FIXME: Throw better!` left in `createEndpoint`.
- `modules/graphql-router/src/searchClient/classifyCatalogueFailureReason.ts` (+ test): new `schema_build_error` code, detected via a `SchemaBuildError`-named marker walked through the same cause-chain search already used for search-client errors (generalised into a shared `findInCauseChain` helper).
- `modules/graphql-router/src/graphqlRoutes.test.ts`: added coverage for `rethrowOnError`'s two branches (the default swallow-and-500 path stays unchanged; the opt-in path rejects, classifiable as `schema_build_error`), previously untested.
- `docs/usage/05-introspection.md`, `.dev/roadmap.md`, `CHANGELOG.md`: documented the new `schema_build_error` code and the correctness fix.

A real catalogue mapping (donor biomarker fields like `ca19-9_level`, `pd-l1_status`) confirmed the actual root cause behind that GraphQL syntax error: no field, nested type, or document-type name is validated or sanitized before being used as a GraphQL identifier anywhere except aggregation field names, so a hyphen or leading digit breaks schema generation with an opaque graphql-js lexer error and no attribution back to which field caused it.

- `modules/graphql-router/src/mapping/utils/findInvalidGraphqlNames.ts` (new, + test): validates every field name and the document type name against graphql-js's own `isValidNameError` before schema generation starts, collecting every offender in one pass rather than stopping at the first.
- `modules/graphql-router/src/graphqlRoutes.ts`: `getTypesWithMappings` now throws a specific, field-naming `schema_build_error` before attempting schema generation when an invalid name is found. Fixed a re-wrapping bug this surfaced: `createSchemasFromConfigs`'s catch previously always replaced a deeper error's message with its own generic one, which would have silently discarded the new field-specific detail; it now passes an existing `SchemaBuildError` through unchanged.
- `modules/graphql-router/src/searchClient/classifyCatalogueFailureReason.ts` (+ test): `schema_build_error`'s `message` now comes from the actual `SchemaBuildError` (naming the specific field, when that's the cause) instead of a fixed generic string; none of these messages carry infra detail, so this doesn't change what's safe to expose.
- `.dev/roadmap.md`: logged the actual fix (general name sanitization plus a raw-to-sanitized resolver lookup, generalizing the dot-only handling aggregation fields already have) as its own roadmap item, scoped separately from this diagnostic since it's a larger, cross-cutting change with real backward-compatibility and name-collision questions.
- `CHANGELOG.md`: documented that `schema_build_error`'s message now names the specific offending field(s).

- `modules/graphql-router/src/mapping/flattenMapping.js`: its returned key is now `fieldName`, matching what every consumer actually calls it. Previously `flattenMapping` returned `field`, and its only consumer, `extendMapping.ts`'s `flattenMappingToFields`, immediately renamed it to `fieldName`; that indirection is exactly what caused the `.field`/`.fieldName` mismatch bug above. Confirmed via a full-monorepo search that nothing else consumed the raw `field` key and this function isn't part of the package's public API.
- `modules/graphql-router/src/mapping/flattenMapping.test.js`: relocated from `mapping/__tests__/` to co-located, updated for the rename.

Fixed two logged style-only tech-debt entries (both pre-existing, not from this session's work): `modules/graphql-router/src/graphqlRoutes.ts`'s network-schema error log and all 33 `CHANGELOG.md` bullets used a space-hyphen-space connector instead of a colon.

- `modules/graphql-router/src/graphqlRoutes.ts`: reworded the network-schema error log to use a colon.
- `CHANGELOG.md`: bulk-replaced the space-hyphen-space title/description connector with a colon across every bullet, verified with `grep -c` before and after.
- `.dev/tech-debt.md`: removed both now-resolved entries.

A real deployment log (`schema_build_error` now correctly naming `donor`'s four invalid fields) also showed the same message printed 2-3 times as it propagated up through `getTypesWithMappings`, `createSchemasFromConfigs`, and `arrangerRoutes`, redundant now that messages are more detailed. `router.ts`'s own `enableDebug` dump already shows the full error (message, cause chain, stack), and `apps/search-server`'s curated summary always shows it once more; the intermediate layers' own printing added nothing either mode didn't already have.

- `modules/graphql-router/src/graphqlRoutes.ts`: `createSchemasFromConfigs`'s and `arrangerRoutes`'s catch blocks no longer print the error themselves when a caller further up will (i.e. whenever `rethrowOnError` is set); `arrangerRoutes` still logs it when `rethrowOnError` is off, since that's the only path where nothing else ever surfaces the failure.
