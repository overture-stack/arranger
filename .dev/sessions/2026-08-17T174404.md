Ran a full monorepo review on request (8 parallel read-only agents, one per area), verified the highest-stakes findings directly, then wrote everything into `.dev/tech-debt.md`/`.dev/roadmap.md` and a new atlas summary doc. Also helped a peer session (iMicroSeq Gateway and Pedigree) trace Arranger's graphql-router/app split, pnpm-migration findings, and Docker/pnpm/corepack conventions as reference for their own pipeline-to-library extraction.

- `.dev/tech-debt.md`: added roughly 48 new findings and corrected 7 stale/incorrect existing entries, organized into the file's existing per-package sections. Highest-severity: `SqonBuilder.not([...])` inverts AND/OR semantics for exclusion filters (confirmed by direct execution, a public builder call produces a silently wrong, broader result); `disableDownloads` is fully wired through config but never checked anywhere, so it does nothing; `disableFilters` protects `/graphql` but not `/download`, so a deployment disabling arbitrary filters can still get a fully filtered export; `SqonSchema` has no recursion-depth limit and throws an uncaught `RangeError` on a small deeply-nested payload, defeating zod's own `safeParse` contract on an MCP endpoint that already has no auth; `MCP_HOST` is parsed but never used to bind the server. A dead legacy admin GraphQL backend with unauthenticated Elasticsearch write mutations (`modules/graphql-router/src/admin/`) was found independently by two separate review agents, still compiling into the published package despite being unreachable. Also: a `columnsToGraphql` crash on an unguarded regex match (runs on every table fetch), every button in `modules/components` defaulting to `type="submit"`, a hardcoded sessionStorage cache key causing colour bleed across catalogues in `NetworkNodesChart`, CSV/formula-injection risk in TSV export, and a real root-cause type mistyping (`ExtendedMappingInterface.type`) that let an existing tracked duplication risk materialize as a silent misclassification bug rather than a compile error.
- `.dev/roadmap.md`: corrected four stale items found during the review (a pointer to a tech-debt entry that was never created for an already-fixed bug, the charts Phase 1.1 "no test script" framing now that one exists and passes, a narrower framing for the `enableAdmin`/access-model item based on the flag's own in-code FIXME, and a missing package in the Changesets `ignore` worked example).
- `.dev/docs/atlas/monorepo-review-2026-08-17.md` (new): review methodology, a "if you only fix five things" triage list, and the cross-cutting patterns the review surfaced (repeated `apiUrl`-forwarding gaps, typed-component-with-no-TS-interface, drifted hand-maintained lookup tables, an unwired feature flag, six stale devctx entries found in one pass). Indexed in `.dev/docs/atlas/index.md`.
- Cross-session: answered three follow-up questions from the iMicroSeq Gateway/Pedigree session (extracting their pedigree pipeline into a library), each grounded in this repo's actual source rather than general advice: how `graphql-router` splits from `apps/search-server` (library scope is deliberately smaller than app scope; env reading and `process.exit` are both app-only); the pnpm migration's real findings so far (still on npm, but `ts-patch`/phantom-dependency risks already prototyped); and Docker/pnpm/corepack conventions (a real prior incident's `corepack prepare --activate` lesson, versus researched-not-prototyped `pnpm deploy` guidance, kept explicitly distinct by confidence level).
