Made the reducer requirement's cross-package dependency part of the requirement, so its fix lands complete in one commit.

- `.dev/docs/arranger-auth/roadmap.md`: Phase 1 item 7 now requires the fix and `apps/mcp-server`'s `build_sqon` regression test to land in the same commit.

Scheduled the two access-control gaps that tech-debt had only recorded, as Phase 1 requirements stated rather than described.

- `.dev/docs/arranger-auth/roadmap.md`, `.dev/tech-debt.md`, `.dev/docs/arranger-auth/debt.md`: Phase 1 gains item 8, binding saved sets to their catalogue and their owner, with the owner taken from the request's trusted context because `saveSet`'s `userId` is client-supplied, and item 9, a per-request shaping hook for configuration. Catalogue binding within a shared sets index is replaced by requiring per-catalogue indices wherever access control is on, which avoids a mapping change and reindex. Narrowing labels per principal needs grants, so only the hook is Phase 1.
- `.dev/docs/arranger-auth/usher-adapter.md`, `.dev/docs/arranger-auth/auth-mode-changes.md`: Phase 2 and 3 items renumbered, and their four citations updated.
