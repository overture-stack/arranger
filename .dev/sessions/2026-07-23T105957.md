Manual work (developer, not agent-authored): renamed all `apps/mcp-server` tool and resource identifiers from kebab-case to snake_case, to match the naming convention used by the majority of actively-maintained TypeScript MCP servers.

- Context: when this MCP server scaffold was started, one of the reference implementations consulted used kebab-case, and general search results at the time treated kebab-case and snake_case as equally acceptable for TS MCP servers. A later look specifically at the most popular, actively-maintained TS MCP server implementations found roughly 90% use snake_case, so this pass brings Arranger's MCP surface in line with the de facto standard rather than leaving it as an outlier.
- Tools renamed (`apps/mcp-server/src/mcp/tools.ts`, `executeQueryTool.ts`): `list-catalogues` → `list_catalogues`, `get-sqon-schema` → `get_sqon_schema`, `get-catalogue-fields` → `get_catalogue_fields`, `execute-query` → `execute_query`.
- Resources renamed (`apps/mcp-server/src/mcp/resources.ts`): `arranger-server-introspection` → `arranger_server_introspection`, `arranger-sqon-schema` → `arranger_sqon_schema`, `arranger-catalogue-fields` → `arranger_catalogue_fields`.
- Updated every in-repo reference to the old names alongside the registrations themselves: tool descriptions and `get-catalogue-fields`/`get-sqon-schema` retry pointers in `executeQueryTool.ts`, the "how to build a SQON" doc comment and cheat sheet in `tools.ts`, validation error messages in `src/arranger/queryValidation.ts`, `docs/usage/06-ai-and-automation.md`'s tool table, and the `integration-tests/mcp-server` suite (`executeQuery.ts`, `readTools.ts`, `spinupActive.ts`'s tool-name assertion list, `index.test.ts`'s suite title).
- `CHANGELOG.md`'s MCP server entry was also corrected while touched: it still listed the pre-rename tool set (`list-catalogs`, `get-catalog-fields`, `search-catalog`), stale on two unrelated axes (the `catalog`→`catalogue` spelling fix and the `search-catalog`→`execute-query` rename, both already shipped separately). Now reads `list_catalogues`, `get_sqon_schema`, `get_catalogue_fields`, `execute_query`.
- Verified: `apps/mcp-server` unit suite passes (71/71). Did not run `integration-tests/mcp-server` (needs a local ES/OS instance, per the same limitation noted in prior sessions); `spinupActive.ts`'s tool-listing assertion and `readTools.ts`/`executeQuery.ts`'s tool-name references were updated to match but not exercised end-to-end.
- Open thread: `.dev/tech-debt.md` and `.dev/roadmap.md` still reference the pre-rename kebab-case names in several entries (the MCP-auth and catalogueId-allowlist tech-debt items, the MCP surface unification and `/docs` MCP coverage roadmap items). Left as-is for this pass since it's prose in planning docs, not code, but worth a follow-up sweep so the docs don't describe tool names that no longer exist. `.dev/docs/build-sqon-tool.md`'s references to `execute-query`/`get-sqon-schema` are design-doc prose predating this rename and are lower priority since that tool is still unshipped.
