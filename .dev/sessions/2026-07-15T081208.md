Investigated two roadmap-adjacent design questions (per-catalogue search engine credentials, per-catalogue config reload) and ran a conservative `catalog` → `catalogue` spelling pass across four packages.

- `.dev/roadmap.md`: added "Per-catalogue search engine credentials via env vars". Confirmed file-based config already supports per-catalogue `esHost`/`esUser`/`esPass`/`searchEngine` (`arrangerRouter()` builds a fresh client per catalogue), but env-var config, the primary deployment path, reads one shared global credential set in `localEnvs.ts`, which has an existing `TODO` to extend it.
- `.dev/roadmap.md`: added "Per-catalogue config reload without full server restart", motivated by local/non-k8s deployments lacking rolling-update downtime avoidance. Confirmed per-catalogue state (ES/OS client, GraphQL schema, Express router) is already isolated and keyed by catalogue ID; the actual gap is Express's static route mounting, which has no mutable registry to swap at runtime. Logged with a proposed incremental path (manual reload trigger, then opt-in file-watching), flagged "to be reviewed before committing to a design."
- `apps/search-server`, `modules/graphql-router`, `modules/types`, `integration-tests`: ran a conservative `catalog` → `catalogue` spelling pass (internal identifiers, comments, doc prose only). Left untouched: `catalogId`/`catalogs`/`catalogCount` and related exported types (external API/config contract), all real config JSON fixtures, and `apps/mcp-server`/`AGENTS.md`/`CLAUDE.md`/`CHANGELOG.md` (out of scope or governed elsewhere).
- `.dev/tech-debt.md`: updated the existing "Inconsistent spelling of `catalogue`" entry to record what's done versus what remains as a breaking-contract rename. Logged two adjacent issues found during the audit: a wrong-key integration test in `readTools.ts` (asserts the right outcome for the wrong reason), and a stale file-provenance comment in `buildCatalogueIntrospection.ts`.

Noted: `.dev/roadmap.md` and `.dev/tech-debt.md` showed as staged in the index without any `git add` run this session; flagged for the developer to check, since staging wasn't intentional on this end.

- `.dev/roadmap.md`: confirmed via code trace, not roadmap text, that a single catalogue with a missing/unreachable index crashes the entire server before startup today (`Promise.all` in `arrangerRoutes.ts` fails fast, propagating to `process.exit(1)` in `index.ts`, discarding any already-healthy catalogues). This is the exact situation in overture-dev (5 catalogues configured, 1 index created, 4 awaiting data from Lyric/Maestro). Expanded "Multicatalog catalogue lifecycle and metadata" with the decided design: single `failed` status with a `reason` code (`index_not_found`/`connection_error`/`mapping_fetch_error`/`unknown_error`) plus human-readable `message`, rather than a separate `pending` status; failed catalogues included by default in server-wide listings (revisit access control once Usher/ABAC lands); `Promise.all` to `Promise.allSettled` as the underlying mechanism. Design only, no implementation yet.

Implemented the core mechanism from that design (BDD: tests written before each change, all passing).

- `apps/search-server/src/arrangerRoutes.ts`: `Promise.all` → `Promise.allSettled`; a catalogue that fails to build gets a stub router (200 + status/reason/message on `/introspection`, 404 elsewhere) instead of crashing the whole process. New `apps/search-server/src/catalogueStatus/` module: `classifyFailureReason` (walks `Error.cause` to classify the real ES/OS failure without ever echoing the raw error text) and `computeAggregateStatus` (`healthy`/`degraded`/`unhealthy`).
- `modules/graphql-router/src/searchClient/fetchMapping.ts`, `modules/graphql-router/src/router.ts`: preserve the original error via `{ cause: err }` on rethrow, so `classifyFailureReason` has something real to classify instead of a generic rewrapped message. Confirmed zero new type errors and zero test regressions in `modules/graphql-router` (213/213 passing) after this change.
- `apps/search-server/src/introspection/`: `buildServerDetails` now includes `status`/`reason`/`message` per catalogue plus a top-level `status` for the server-wide aggregate; `/introspection/:catalogId` (not a new bare route) doubles as the per-catalogue metadata endpoint for failed catalogues.
- `apps/search-server/src/server.ts`: new readiness endpoint at configurable `health.readyPath` (`READY_PATH` env var, default `/ready`, mirrors the existing `pingPath` pattern), `503` only when `unhealthy`; liveness (`/ping`) deliberately untouched.
- Fixed the same-session-discovered standalone bug alongside this: `getConfigFromFiles` was silently swallowing a malformed catalogue config into fallback defaults instead of surfacing the failure; now rejects, letting the existing (already-correct) per-directory isolation in `configs/index.ts` actually run. Also distinguished "no config directory" (legitimate env-only fallback) from "config directory present but broken" (now fails loudly) in `loadAllConfigs`.
- Found and fixed a second, unrelated pre-existing bug while verifying: `apps/search-server`'s `test` npm script glob only matched test files exactly two directories deep, due to `sh`'s non-globstar `**` handling, silently skipping shallower/deeper test files with no error signal. Changed to bare `tsx --test` (recursive auto-discovery). The identical bug exists in `apps/mcp-server`; logged to `tech-debt.md`, not fixed there (out of scope this session).
- All new and existing tests passing: 31/31 in `apps/search-server`, 213/213 in `modules/graphql-router`. Typechecked both packages; confirmed (via `git stash` comparison) zero new type errors, only pre-existing, unrelated ones remain.
- All changes are unstaged in the working tree; nothing committed, per standing instruction.

Caught after the fact: `READY_PATH` was missing from `apps/search-server/.env.schema` (the documented env var reference), even though the code path was correct. Added it. Extended `AGENTS.md`'s existing "Env vars" convention bullet with a checklist note: new env vars must also be added to `.env.schema` (and mcp-server's equivalent, if that's the app being changed).
