Synced agentics: `AGENTS.md`'s `synced` SHA had gone unresolvable (agentics rewrote its own history for a name scrub); ran the full upstream diagnosis per `upgrading-adoption.md`'s troubleshooting section rather than a diff.

- Content is already at parity with the current template (0.9.0 -> 0.10.0 added nothing that gets copied locally; those changes live in `conventions/*.md`, correctly treated here as live pointers). Retagged to `0.10.0 | synced: 6313e587222772d8ef9c111ae3d4637678acff89`.
- New convention worth noting: `code-style.md` § Git now bans AI-tool attribution trailers in commits/PRs. No local file carries this text (git conventions aren't inlined into `AGENTS.md`), so nothing to edit; applying it going forward.
- Found a real conflict between the newer `code-style.md` § Property ordering (alphabetize object literal properties, all nesting levels) and this codebase's established, consistent pattern for MCP SDK config/content objects (`title`/`description`/schema in `registerTool`/`registerPrompt` calls across `tools.ts` and `prompts.ts`; `type`/`text` in every `content` array). Left these as-is rather than resorting one file out of step with its siblings; flagged to the developer as a candidate for `.dev/agentics-overrides.md` if the codebase should keep favouring identity-then-schema/discriminant-first ordering for these SDK-facing shapes over strict alphabetization.

`prompts.ts`: fixed an inconsistency left by the in-progress em-dash removal. `code-style.md` § Dashes bans space-hyphen-space too, not just em dashes: `Option A —`/`Option B —` had been converted to `Option A -`/`Option B -` rather than the colon form used for every other heading in the same file. Also added TSDoc to `registerPrompts`, the module's sole export, which was missing one per § TSDoc for exported symbols. `tsc`, eslint, and the `apps/mcp-server` unit suite (71/71) all clean.
