Root-caused a Jenkins-only test failure to an incomplete commit, then adopted supertest across the six files that were hand-rolling HTTP test servers.

- `apps/search-server/src/arrangerRoutes.ts`: verified the still-uncommitted `/:catalogueIdentifier` documentType-fallback fix (missing from `c13c6131`) against the live suite and against introspection's equivalent handler, then reproduced the exact Jenkins failure locally by testing `c13c6131` on its own. The gap traces to that commit wiring the shared `findCatalogueByIdentifier` helper into `introspection/index.ts` but never into this file, despite adding tests for both call sites.
- `apps/search-server/src/arrangerRoutes.test.ts`, `apps/search-server/src/introspection/introspectionRoutes.test.ts`, `modules/graphql-router/src/formatError.test.ts`, `modules/graphql-router/src/disableGraphQLIntrospection.test.ts`, `modules/graphql-router/src/enableGraphQLBatching.test.ts`, `modules/graphql-router/src/__tests__/disablePlayground.test.ts`: replaced manual `app.listen(0)`/`server.address()`/tracked-close boilerplate (the axios-based variant in the graphql-router files) with `supertest`. A bare `Router` isn't valid `request()` input and breaks its own 404 fallthrough, so every call wraps the router in a plain `express()` app first.
- `apps/search-server/package.json`, `modules/graphql-router/package.json`: added `supertest`/`@types/supertest` as devDependencies.
- Confirmed the roughly 104 pre-existing `tsc --noEmit` errors in `modules/graphql-router` are unrelated to this change: the untouched `c13c6131` baseline shows the same count. Consistent with the already-acknowledged `noCheck: true` TODO in `tsconfig.release.json` and the tracked Apollo Server 3 debt entry; not logged as a new entry.
- Proposed a convention gap to agentics: neither `testing.md` nor `session-discipline.md` requires actually running the full test suite at any checkpoint (after a unit of work, before commit, before session end). `session-discipline.md`'s "Refinement passes" section already has the right three-checkpoint shape but only for prose, not test execution, and doesn't account for an involuntary session end (a usage or context limit) that never reaches a voluntary "before ending a session" checkpoint at all. Posted as a handshake to the shared agent index's Requests board since the registered `agentics` session name had gone stale.
- `modules/types/src/tools/graphqlNameFns.test.ts`: added `fast-check` property tests for `sanitizeGraphqlNameSegment`/`sanitizeGraphqlFlatName` (valid-GraphQL-name shape and idempotence hold for any input), as a scoped pilot rather than a broad adoption. While reasoning through invariants for this function, found that two different dotted paths can sanitize to the same flat name (`a.b` and `a__b` both produce `a__b`, since the dot-to-`__` step and the character-sanitizing step compose): not asserted as a property (it would legitimately fail), flagged to the developer as a possible real collision hazard rather than fixed or logged as debt unprompted. `modules/types/package.json`: added `fast-check` as a devDependency.
