Triaged a batch of security-review candidates (SQON validation gap, `getAllData` logging/error-handling issues, per-request logging, auth fail-open risk) brought in from another session; folded the design-level ones into existing roadmap items rather than logging them as separate entries.

- `.dev/roadmap.md` ("Config validation with structured errors and tests"): added a security framing note: silent fallback on invalid config is an OWASP A08:2025 (Software or Data Integrity Failures) risk, not just a UX issue; correct behaviour is refuse to start, not silently apply a partial config
- `.dev/roadmap.md` ("Auth and field/record-level access control"): added two non-negotiable safety defaults regardless of how the rest of the ABAC design resolves: fail-closed on auth enforcement failure (OWASP A01:2025 if accidentally fail-open), and every access denial must produce a structured `{ userId, resource, reason }` log entry from the first implementation
- `.dev/roadmap.md`: added new item "Structured request logging as a prerequisite for ABAC" (Architecture section, after Observability): confirmed no structured per-request log exists anywhere in the query resolvers or `apps/search-server` today; scoped as one log event per query request (`catalogId`, `queryType`, `sqonSize`, `hitsReturned`, `durationMs`, `userId` once auth lands), explicitly lighter-weight than the full Observability item (no OpenTelemetry/metrics endpoint needed)

Verified the SQON-not-validated-at-ingress candidate against actual code (confirmed accurate: `resolveHits.js`, `resolveAggregations.ts`, and `buildQuery/index.js` all take `filters` straight into query-building logic with no schema validation) and found a related bug while checking whether the network path's `convertToSqon` could be reused as the fix: its validation check is inverted (`if (result.success) throw`), so it currently rejects valid SQONs and passes invalid ones through unchanged. Left that specific bug alone per instruction (already flagged via a PR comment elsewhere); no tech-debt entry written for it here.
