Ran an adversarial pass over this session's own position on the `matchNothing()` addition, at the developer's request, each of the two sessions attacking its own stance from the other's vantage. The exercise changed the recommendation rather than confirming it. No code changed.

- **The conclusion that matters: the agreed additive boundary is right as a scoping decision and wrong as a resolution.** Shipping a safe constructor routes one caller around a defect that remains reachable and ungated everywhere else, and it makes the defect *harder to find*, since once a blessed constructor exists nobody writes the negation form and nobody trips the inversion. That reduces the probability of discovery without reducing the probability of harm, which for a security-relevant inversion is arguably a net negative. Both sessions reached this independently from opposite directions.
- Measured how wide the exposure actually is, which is wider than "someone hand-writes a literal": **`SqonSchema` accepts every unsafe form**, including `not` with empty content, `not[and []]`, `and []`, and `or []`. So there is no gate at any validation boundary in the repo, and the three consumers that `safeParse` SQON they receive (`mcp-server/src/arranger/queryValidation.ts`, `integration-tests/import`, and the module's own tests) would pass it straight through.
- Recorded the two candidate closures and why neither is trivial. Having `reduceSqon` throw rather than silently emit `must_not: []` when pruning empties a `not` is the narrower change. Having `SqonSchema` reject a combination with empty content is the wider net, catching the form before any builder or compiler sees it, but `compileFilter` in `graphql-router` constructs `{op:'and', content:[]}` as its own default for an absent filter, so the schema option cannot land without changing this package at the same time. Worth knowing before anyone proposes it as the simple fix.
- **Withdrew a request made of the module owner, which turned out to be unfulfillable.** This session had asked their pinning test to assert the emitted Elasticsearch. `modules/sqon` has no dependency on `graphql-router` and correctly should not, since that direction is circular, so the only way to satisfy it is a hardcoded ES string: a test in one package asserting another's behaviour with no dependency edge to make it fail correctly when the compiler changes. It would go red on a legitimate change to code its owner does not control. The ES assertion belongs entirely on this side.
- **Found that the composition test volunteered by this session would pin a structure Phase 0 is meant to change.** The plan was to assert that the empty `terms` clause survives `compileFilter` composition. But the audit's own recommended fix for the `global` aggregation escape is to keep the client and server filters separate through to aggregation building, which changes exactly what `compileFilter` emits. The test would go red on the fix this project has been advocating and would read as a regression when it is progress. It needs to assert a semantic property rather than a structural one, which is not currently possible without a live cluster. No clean answer yet; recorded as open rather than written as a structural test and called covered.
- Conceded that the verification plan was weak in the way this session has criticised elsewhere. "All suites green" is near-vacuous evidence for a pure addition, since nothing calls the new function and therefore nothing can move, and the relevant coverage is known poor (every existing test passes a null server-side filter, `/download` has no tests at any layer, `components` has no rendering tests). Also caught a stale-`dist` trap in the plan: "build first" is insufficient, since a meaningful diff needs the unmodified module built and captured as a baseline before the change, then rebuilt after. "Build twice" is the step that makes it valid.
- Sharpened an earlier claim that a new export is "still additive". `modules/types/src/sqon.ts` is a bare `export * from '@overture-stack/sqon'`, so a new export lands in `@overture-stack/arranger-types`' published public API with no edit to that package and no version-bump reasoning applied, and no export-snapshot test exists to catch it. "No change to existing behaviour" and "no change to the published API surface" are different claims that had been conflated.

Coordinated a definitive resolution with the SQON module's owner, plus where the documentation belongs. Still nothing written: the remaining changes touch published surfaces (`docs/`, two READMEs) and await the developer.

- **The fix relocated entirely into this package, which is the outcome of the adversarial pass.** `getDefaultServerSideFilter` returns `{op:'not', content:[]}`, the repo's own "no access control configured" sentinel, and that is byte-identical to what a deny filter becomes after `reduceSqon` prunes it. Verified: same node, same emitted `{"bool":{"must_not":[]}}`. So "allow everything, deliberately" and "deny everything, corrupted" are the same value, which is why no downstream guard can work and why both previously-considered fixes fail. Rejecting a `not` with empty content would reject the repo's own default and break every deployment without access control configured; making the compiler emit match-none for `not[]` is wrong in general, since `NOT(∅)` meaning "exclude nothing" is logically correct. The compiler is fine and the sentinel is overloaded.
- The change: give allow-all a shape that pruning cannot produce (a `not` wrapping a match-none leaf, since pruning only removes empties and never invents leaves), then guard at `compileFilter`. One sentinel file, one guard, three call sites (`resolveHits.js:269`, `resolveAggregations.ts:97`, `resolveSets.js:77`), all in `graphql-router`. Nothing in `reduceSqon`. Two implementation caveats: the emptiness check must be recursive, since `{op:'and',content:[{op:'and',content:[]}]}` compiles to match-all with a non-empty top level, and `compileFilter` uses `{op:'and',content:[]}` as its own client-side default, so that needs the same treatment or an explicit exemption. This is P0-a as already recorded; what the exercise added is that the sentinel is the blocker rather than the guard, and that `matchNothing()` cannot substitute for it.
- **A third documentation defect, previously uncatalogued.** `modules/graphql-router/README.md:194` states the filter callback returns "a `SqonNode` (or `null` for no filter)", which documents the nullish path that silently disables access control, since `compileFilter` turns a falsy filter into match-all. Combined with the `field`-instead-of-`fieldName` example at `:206`, that README currently teaches both failure modes on the only mechanism it documents.
- Proposed a documentation division on the principle that a fact belongs wherever the behaviour lives, with the other side cross-referencing rather than restating: construction-side semantics and the pruning consequence to `modules/sqon/README.md` (which currently has zero mentions of `not-in` or `some-not-in`) plus the constructor's doc comment; emitted-Elasticsearch semantics, the published operator table, and the filter-authoring guidance to this package. Explicitly argued that emitted-ES semantics are *not* the module owner's to document, since they do not own the compiler, which is the same layer boundary that retired the earlier emitted-ES test request.
- **The structural proposal, which matters more than any individual correction:** pin the published operator table to executed output. It was wrong because it was written from what the operator names suggest, so correcting it does not stop a recurrence, and two of its four membership rows being wrong makes the other two unverified rather than trustworthy. Proposed a test in `graphql-router` emitting ES for every operator on a flat and a nested field, with the table written from its output and the test named so that editing the doc without touching the test is visible in review. Closes the class rather than the instance, and answers the question both sessions keep hitting: where a reader finds out what an operator actually does, for which the current honest answer is "execute it".

Implemented the empty-filter fix in `graphql-router`, at the developer's direction to refocus on the security work. All suites green; the canonical record is the `tech-debt.md` entry, now marked fixed.

- Before writing, flagged that the two problems in play are not equally severe and that the one designed over the preceding days is the lesser. The empty-filter class needs a filter author to produce a degenerate value, which is a mistake; the aggregation escape needs nothing beyond an ordinary client and defeats a *correctly written* filter. Recommended doing the lesser one first regardless, because the integration test is the shared instrument that will then measure the greater one, and there is currently no way to demonstrate any of this fixed.
- Two changes, both in this package: the allow-all sentinel now carries a leaf so its shape is unreachable by pruning, and `compileFilter` rejects a server-side filter that is absent or has no leaf clause at any depth. It validates only the server-side filter, since a client filter restricting nothing is an ordinary query while a server filter restricting nothing is a failure wearing the same shape, and it throws rather than substituting a deny, because a silent deny is indistinguishable from a query that legitimately matched nothing.
- **The eight resulting test failures were the finding rather than collateral.** Every one passed `() => null` or `(() => undefined) as never` as its filter, which is exactly the pattern the audit named as the reason three escape routes survived review. They now state "no access control" explicitly. The `as never` casts disappeared with them, having existed only to defeat the type system's objection to the original value.
- **Found a defect that was not previously known, as a side effect.** The ES `global` wrapper is applied only when removing the aggregated field's clauses changes the query; `removeFieldFromQuery` returns `null` for a bool with an empty clause array, and `null` compares unequal to an empty bool, so **an empty query caused every aggregation on every field to be wrapped spuriously**. Harmless in itself, since with no filter there is nothing to escape, but it means the wrapper was far more widespread than its purpose requires. Two tests asserted the wrapped shape and were pinning the defect; they now read either shape, since their subject is `nestingPrefix` handling rather than wrapper structure.
- Established the fix does not change emitted queries for deployments that already had access control, by reconstructing what the old code produced rather than trusting that the suite passing meant nothing moved. The only divergence is the empty-query case, and there the change removes a wrapper that served no purpose. Worth recording as method: the guard made the old path unreachable, so a naive before-and-after was impossible and the baseline had to be hand-built.
- P0-14 downgraded rather than closed: both read paths now fail closed on a missing callback, so what remains is two different error messages for one misconfiguration rather than one path silently unfiltered.

Closed the aggregation escape, then ran an adversarial pass over the wider package including this session's own changes. Two new canonical entries; the rest is below.

- Closed the escape by restoring provenance at the one place it is lost. `resolveAggregations` now compiles the server-side filter separately as well as composed, and `buildAggregations` re-applies it after field-removal has stripped the aggregated field's clauses. The caller's own facet filter stays dropped, so selecting a value still does not collapse that facet; the server's filter is no longer removable. Verified across every amplification previously recorded: buckets, `top_hits`, `stats`, `cardinality`, `bucket_count`, and a nested access field all now carry the restriction, and the client-filtered case still behaves as a facet should.
- Nearly reported a leak that was an artifact of the test rather than the code: the nested case asserted on a clause keyed `access` when a nested clause is keyed `donors.access`, so the assertion could not match even when correct. Caught by re-running with the right key rather than by reasoning about it.
- The fix required a public export that did not exist. Returning nothing from a filter callback now throws, so consumers need a way to say "allow everything", and `getDefaultServerSideFilter` was not reachable from the package root. Exported it, and rewrote the README's server-side-filter section, which carried three separate defects: a documented `null` return that now throws, a worked example using `field` where SQON requires `fieldName`, and a claim that the client "cannot remove or bypass" the filter, which is a guarantee not currently provided on every path. The last was narrowed to state which paths compose the filter rather than to enumerate which do not.
- **Adversarial probing of the new guard found no way past it.** A client filter cannot widen the composed result: negations, empty and deeply-nested-empty combinations, and a filter naming the access field itself all leave the restriction intact, the last intersecting rather than widening. Malformed shapes throw.
- **Found that aggregation arguments are read positionally while GraphQL defines argument order as insignificant.** Writing `top_hits(size:, _source:)` rather than `top_hits(_source:, size:)` silently loses both, since each positional lookup lands on the other argument and falls back. Six sites share the pattern in one file; `top_hits` is the only one reachable today because it is the only one taking two arguments, and the other five acquire the same defect the moment a second argument is added.
- **Found that `MAX_AGGREGATION_SIZE` is a default rather than a maximum.** A caller-supplied `max` replaces it verbatim with no clamping, and `top_hits(size:)` multiplies against it. What prevents an unbounded request today is Elasticsearch's own `search.max_buckets`, which is incidental protection rather than designed and disappears wherever that setting has been raised. The same expression also carries a dead default: `|| 1` produces a number and the next line reads `.value` off it.
- **A risk in this session's own uncommitted work, recorded rather than resolved.** The new allow-all sentinel emits `terms` on the `_id` meta field with an empty value list. That should be inert, but it has only been verified as emitted structure, never against a running cluster, and it is now on the path of every deployment that configures no access control. If any supported Elasticsearch or OpenSearch version rejects a `terms` query on `_id`, that breaks every such deployment on every request. It is the first thing the planned integration test should assert.

Ran the live-cluster verification the whole sequence had been waiting on, Docker having become available, and closed the export bypass on the strength of it.

- **Cleared the risk this session had introduced.** The new allow-all sentinel emits `terms` on the `_id` meta field with an empty value list, which had only ever been checked as emitted structure. Against Elasticsearch 7.17.28 it is accepted and returns every document, identical to the shape it replaced. That was the one outcome that could have broken every deployment configuring no access control, on every request.
- **Demonstrated the aggregation escape and its closure on real data**, with a working negative control rather than an assertion. Against an index holding two public and two restricted documents, with a principal restricted to public: without the fix the facet returned all three access tiers and `top_hits` inside the escaped bucket returned both restricted documents by name; with the fix the facet returns only the permitted tier and no restricted document is reachable. The control is the part that matters, since it proves the harness can detect the leak it reports closed.
- A first attempt at that control produced a false pass, and the reason is worth keeping: `top_hits` was nested as a sibling of `buckets` when the code reads `buckets.top_hits`, so nothing was emitted and both arms looked clean. Caught because the control arm did not move, not because the output looked wrong.
- **Demonstrated the export bypass rather than inferring it.** Same principal, same client filter, the records path returned two documents and the export path returned four, its query carrying no access clause at all. Then closed it: `getServerSideFilter` is now threaded from `router.ts` through the download chain to `getAllData`, which composes via `compileFilter` like every other read path. Verified both directions on the cluster, since a fix that over-restricts would be its own defect: with an access filter the export returns only permitted documents, and with no access control configured it still returns everything.
- Chose an explicit parameter over reading the callback from the Express request context, on the reasoning that the callback is router-scoped configuration while the context is per-request state. Putting it in the context would give an access-control callback the affordance of being host-injectable per request, and `addContext` is documented as the host application's own injection point.
- Eight further test failures, again all of the same shape: every `getAllData` call site passed no filter, so the export tests had been exercising the very bypass being fixed. Now explicit.
- **A claim recorded earlier in this project is imprecise and should not be repeated.** The `/download` route was described as having no tests at any layer. The HTTP route has none; `getAllData` has eight, which is what surfaced above. The accurate statement is that the route is untested, not the export path.

Closed the federation omission with the small change, and rewrote its `tech-debt.md` entry around what the change deliberately does not solve.

- Threaded `getServerSideFilter` from `graphqlRoutes` through `createSchemaFromNetworkConfig` and `createResolvers` into the network resolver, which composes it into the variables sent to remote nodes. Cheaper than expected because the resolver already received `context` and `queryVariables` was already the object carrying `filters`; the work was passing a value, not building plumbing.
- **Recorded as raising the floor rather than creating a guarantee.** A remote receives the filter as an ordinary client SQON, so a cooperating Arranger intersects it with its own and a remote that ignores it applies nothing, indistinguishably. The entry now says that in its first lines, because the tempting summary is "federation composes the filter now" and that reads as enforcement.
- Three things left open there, the first being what makes the others hard to reason about: merged aggregation buckets carry no per-node provenance, so a caller cannot separate an enforced local contribution from an unenforced remote one and the fix improves the request while leaving the response ambiguous; the passthrough-header default forwards no identity and nothing warns at boot; and the trust model itself is undecided, which is a product decision rather than a patch.
- A typecheck caught an import that an editing script had silently skipped, because the guard tested for the wrong line. The suite was green without it, since the failing path has no test, so tests alone would not have caught it. Baseline confirmed unchanged otherwise: 54 pre-existing errors before and after, none in the files touched, and the one annotation flagged in a changed file is identical at `HEAD`.

Relayed the full change set to the Usher session and ran the conformance case they specified. Their design of that case is the most useful thing to come back.

- **Ran a zero-grant principal against a non-empty index across all four read paths, and it passes on each**, with the enforcement clause asserted present in the emitted body rather than inferred from an empty result, and with a positive control on the same records in the same run. Aggregations included `top_hits` inside the bucket so the row covers document retrieval and not only counts.
- **The second assertion earned its place immediately.** Without it, "deny returns nothing" on the aggregations row would have been satisfied by an aggregation that emitted no `top_hits` at all, which is exactly the false pass hit earlier in this session when the harness nested `top_hits` as a sibling of `buckets` rather than under it. The Usher session's shape catches that class by construction rather than by someone noticing the control failed to move.
- Their answer on field arity is better than the question. This session framed single-valuedness as an index-side property that the plugin could only document; a catalogue's own configuration declares array-ness, which makes it verifiable at startup instead. The refinement worth keeping generally is that such a check must fail startup rather than warn, because a permissive-direction flip that nobody can see in a diff is not something a log line catches. Same reasoning that made `compileFilter` throw rather than fall back.
- Their framing of the federation gap is sharper than the one recorded here: an authorization service that cannot verify enforcement downstream should not silently imply it did. That makes marking remote contributions distinctly the floor rather than one option among four. Left the write-up describing the gap rather than proposing a posture, since the trust decision is with their developer.
- **Corrected an assumption of theirs that would have cost real work.** They had written off a stale investigation covering "export and sets" on the basis that four escape routes were closed. Export is closed; the `sets` root connection is not, and the guard added this session does not help there, because a document-shaped access filter evaluated against a structurally unrelated index is semantically void rather than enforcing. Told them not to drop that half.
- Marked every section of the update as closed-and-safe-to-commit or open-and-undisclosed, at their request, since they had themselves written a reproduction into committed `.dev/` earlier and were rewriting those passages. Applying the prescriptive-versus-diagnostic split to an outgoing message rather than only to files was straightforward and worth repeating.
- Recorded a caveat on the result so it is not over-read: one cluster, one Elasticsearch version, one index shape, all fields flat. Nested access fields and OpenSearch are untested and the shared corpus will need both.

Two peer sessions audited this session's own devctx and found staleness it had missed. Both reports checked against on-disk state rather than accepted; all of it was real.

- **A half-removal created by this session was the worst of it.** The empty-grant entry was marked fixed with a closure block written above the original text; when the developer said resolved debt should leave the file rather than accumulate, the closure block was removed and the original left underneath, under the heading given to it. `tech-debt.md` therefore carried an untitled block still reading `**Severity:** high` and "live today" about something already fixed. A reader landing on it would have found a live high-severity finding. Removed entirely, the reasoning being preserved in `arranger-auth/debt.md` and `roadmap.md`. **The general shape is worth watching for: a rule that resolved items leave the file makes partial removal the characteristic failure**, because the half that stays is the half that asserts the problem still exists.
- Three further superseded passages, all flagged by a peer and all confirmed: a request that `modules/sqon`'s pinning test assert emitted Elasticsearch, which had been **withdrawn as unfulfillable** since that package has no dependency on `graphql-router`; P0-a still prescribing a typed deny-all value, which is not the route the fix took; and the matching `tech-debt.md` fix line. P0-a's closure now records that it was done *not by the route it proposed*, since a closure silently matching the original prescription would hide that the diagnosis moved from the deny side to the allow side.
- Found one the peer had not: P0-b still described all three filter-escape routes as open. Marked done with the federation qualification attached.
- Worth noting how the two staleness sets differed. This session fixed the prose claims that export, aggregations and federation were broken; the peer flagged the constructor and direction lines. Non-overlapping, both real, and neither would have found the other's, which is an argument for the audit having come from someone with the files in their tree rather than from a re-read by the author.
- A peer's generalization, better than the one offered from here: prefix, start time, working directory and socket path all correlate with identity without conferring it, so the failure is treating any launch-mechanics artefact as identity, not just prefixes or timing specifically.
- The Usher session returned a coverage table rather than accepting the flat caveat, and it sharpens the result: the near-term integration runs Elasticsearch with a flat resource key and therefore sits inside what was verified, while the other Overture deployment runs OpenSearch with access fields nested at several depths, so both untested conditions land together on that one and cannot be closed independently. `graphql-router` carries both clients, so this is a live configuration rather than a hypothetical.
- A follow-up audit caught this session misreporting its own work, and the cause is a shape rather than a slip. Two operations were applied to one entry in sequence, rewriting its fix line to say superseded and then removing the entry entirely; the second overwrote the first, and both were then reported as separate outcomes because the report tracked what each *edit* did rather than what the file *ended up* saying. **That is the orphaned-heading failure one level up: a half-state left in a report instead of in a file, and both come from tracking operations instead of end state.** The check that catches it is reading the file afterwards rather than trusting the edit sequence.
- Fixed a structural contradiction of the same family: the empty-grant entry, whose own first line records it as resolved, was sitting under `arranger-auth/debt.md`'s `## Blocking` heading while the file carried an empty `## Resolved` section. Moved rather than annotated, because the section already existed for that purpose and its name states the contract, whereas a note explaining that Blocking retains resolved entries would be a second rule to remember on a file whose value is that scanning headings gives the right answer. `## Blocking` now holds only the two entries that genuinely block.
- The reviewing peer unified the two failures into one rule worth carrying: **an edit sequence is not evidence about a file, in the same way a launch artefact is not evidence about identity.** In both cases the thing that feels like it carries the information is a byproduct of how you arrived, and only reading the end state settles it. That covers the orphaned heading, the misreported removal, and the prefix-and-timing identity errors with a single mechanism rather than three separate cautions.

Documented a sets consumer this repository had no record of, at the developer's direction, and had the draft corrected five times by the consumer's own session. Record is at `.dev/docs/sets-consumer-singularity.md`, indexed under a new "Consumer integrations" heading in the atlas.

- **The finding worth carrying: Arranger is not the only reader of the `clinical_centric` data index.** Singularity reads it directly with its own credentials while portal-ui has Arranger configured to serve the same index, so any access control placed in Arranger covers one of two paths. That is the dual-layer enforcement problem with a second consumer nobody had named, and it bears on the Usher work. Recorded as the expected shape of the architecture rather than a defect: Arranger is a query and aggregation layer, not a data-access gateway, so a second service reading the index directly is normal.
- The developer's framing that sets are under-developed rather than abandoned is now the first thing `atlas/roadmap/sets-feature.md` says, ahead of the analysis, so proposals start from build-out rather than retirement. Corrected a factual error in the same file while there: it claimed there is "no query to fetch a set back", when `createSetsType` passes the sets index through `createConnectionResolvers` as a pseudo-catalogue and the schema exposes a full connection over it.
- **A concern raised on this side was wrong and is downgraded.** Set IDs had been treated as travelling outward and possibly functioning as bearer authority. In portal-ui's flow the ID lives in an in-memory `const` inside one async function and never reaches a human, a URL, a filename, or storage; the identifier that appears in a download URL is Singularity's archive ID.
- **The same mistake twice, which is the reusable part.** Two of the five corrections were the same failure: treating two claims as one. "Not exposed" was written as "not a durable handle", when the set document persists and only exposure was established. And a carefully quarantined open question was re-imported one section later by the ambiguous phrase "its index", which a reader could take to include the sets index and conclude a second direct reader had been confirmed. An uncertainty returning in the clothes of a structural claim is worse than an open question, because it no longer reads as one.
- Reversed a judgement on withholding detail. An index name had been omitted as though it were a hostname; it is shipped to browsers as a public build variable, and withholding it was what created the ambiguity above. **The specific name was doing work rather than being detail**, which is the case where over-caution has a cost rather than being free.
- One question left open rather than guessed, on the consumer session's insistence: whether Singularity replays a set's materialized `ids` or re-runs its stored `sqon`. They declined to answer since their whole contribution is handing over an opaque identifier, and their deployment-config lean toward `ids` is recorded as unverified inference. Standing guidance recorded with it: until settled, design against `ids`, being the more constraining reading.

The consumer session then retracted its own identity, and the handoff produced both a verification and a scope correction that changed what the document covers.

- **Its developer told it that it did not own the surface it had answered for**, after it had confirmed it did. It handed off to the owning session, which re-read every claim against current code rather than inheriting the first read, and confirmed them. Attribution is restored on that verification rather than on the original say-so, and future portal-ui questions route to the owner directly, the intermediate hop being what went wrong.
- **How it retracted is the part worth keeping.** It split its claims by which artefact each was read from rather than retracting uniformly: application-code answers unverified, deployment-configuration answers likely sound. Uniform retraction would have discarded the two answers most likely to be correct, which are the two carrying the structural finding the document exists for. Confidence markers were added inline at each affected claim rather than only in the header, since a reader arriving by link or grep skips the header, and "no access control anywhere in this path" is a sentence someone designs against.
- **The material addition came from the owner and narrows the document's scope.** Portal-ui has a second download path, for environmental data, that issues a plain Arranger query, assembles the file in the browser with JSZip, and imports nothing from Singularity. Everything recorded describes the clinical path only, and the open `ids`-versus-`sqon` question does not arise on the other one at all. Two Arranger-side implications, marked as inference: that path's enforcement point is the ordinary record-fetch resolver rather than the export route, so it inherits the composition that path already applies; and no limit configured on Arranger's `/download` route constrains it, since it never calls that route.
- **Three instances of one failure mode in three rounds, now from both sides.** A claim written at a wider scope than its evidence supports: "its index" covering two indices, "not durable" covering two properties, and "portal-ui" covering an application when the evidence covered one of its data paths. The last was the reporting side's, caught by its successor. The fix is the same each time, making a claim's scope match its evidence's scope, and the reason the class is expensive is that every instance reads as settled rather than provisional.
- **A third case completes a generalization this session had been circling.** An edit sequence is not evidence about a file; a launch artefact is not evidence about identity; and a working directory is not evidence about which label a developer had in mind. All three are byproducts of how you arrived, and only the end state, or the developer, settles it.
- **That generalization was going to be proposed upstream, and the peer inverted it into something better.** It read the file the proposal was aimed at and found the rule already stated there, in the imperative, in a file it had a standing instruction to consult; the failure happened because the file was never opened. **A convention that is skipped and a convention that is missing look identical from outside and have nothing in common as problems**, and writing it down again addresses only the second. The proposal upstream should therefore be the compliance question, not the wording, and it arrives with evidence rather than as a hypothesis.
- **What is genuinely missing is a symmetrical pair, one half of it this session's own defect.** The stated rule says a working directory is not evidence without saying what to do when there is no positive evidence either, so the responder half is: name the artefact you have rather than the label you infer. The asker half is this session's: the opening question was "if you are not X, say so", which asks for a conclusion, requires an introspection the responder had not performed, and is satisfiable by silence. Asking which repository the working directory is in would have surfaced the mismatch in the first round from something unmissable, with the judgement left on the asking side. **Responder volunteers the artefact, asker requests the artefact**; either half alone would have caught this.
- Adjacent observation on the shared index, neither session's to fix and already routed by the peer to its own developer: one entry's `owns` field is a bare organization prefix rather than a repository, so a session checking itself against it has nothing repository-shaped to compare with. Longest-prefix resolution means it does not shadow the more specific entries beneath it, so nothing is broken. The sharper half is that the peer's workspace has no entry at all, which under the registry's own stated defaults makes it a task thread owning nothing, so the correct first-round answer was reachable from the defaults alone.
