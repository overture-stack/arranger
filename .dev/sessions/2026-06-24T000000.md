Added `getAllData` to the `graphql-router` utils barrel export (was missing, causing `ERR_PACKAGE_PATH_NOT_EXPORTED` for a downstream consumer); extended the `integration-tests/import` tech-debt entry with two explicit gaps.

- `modules/graphql-router/src/utils/index.ts`: re-exported `getAllData` as a named export; must evaluate whether this is a desirable pattern for future versions
- `.dev/tech-debt.md`: `integration-tests/import` entry extended with two TODOs: verify each `exports` subpath by name (not just the root), and document what each exported method is and what it is for

Improved SQON-generation success for smaller LLMs calling the MCP `execute-query` tool, two enhancements targeting the root causes documented on 2026-06-11.

- `apps/mcp-server/src/mcp/executeQueryTool.ts`: rewrote the `sqon` input parameter `.describe()` into an inline few-shot. Spells out the GROUP vs LEAF shapes, that a leaf nests `fieldName`/`value` inside `content` (the key is `fieldName`, never `field`, and they never sit beside `op`), a correct "donors are Female" example, the exact WRONG flat-leaf shape to avoid, and the empty-root `{"op":"and","content":[]}` for unfiltered queries
- `apps/mcp-server/src/mcp/tools.ts`: replaced the `get-sqon-schema` tool's `text` content (previously the raw `zodToJsonSchema` dump) with a compact, ASCII-only SQON cheat sheet: grammar (Group/Leaf), field operators grouped by applicability, the `filter`-op `fieldNames`-array exception, pitfalls (flat leaf; booleans as strings), a symbol-alias note, and seven worked examples plus the WRONG example. The full machine-readable schema and operator metadata stay in `structuredContent`
- Verified all seven cheat-sheet/param examples parse against the real `SqonSchema.safeParse`, and the WRONG flat-leaf example is rejected (one-off script, not committed)
- Updated `integration-tests/mcp-server/test/readTools.ts` test 2 for the new prose `get-sqon-schema` text
- `npm run test -w apps/mcp-server` green (67 tests); `tsc -p apps/mcp-server` clean

Reworked the cheat sheet again after a local-LLM retest with gemma-4-e4b still produced invalid SQON; every SQON structure the model itself suggested was invalid (fieldName beside `op`, a bogus `"in"` key on a `not` leaf), so only the presentation ideas were incorporated, not the structures.

- Reworked the `get-sqon-schema` cheat sheet (`tools.ts`): front-loaded a "THE MISTAKE TO AVOID" block showing the CORRECT leaf next to the two real malformed shapes, added a copyable leaf template, a 3-step build recipe ("always wrap in a Group root"), a multi-value `in` note, a negation note (`not` group or `not-in`), and an OR example; reframed the worked examples as "Natural language to SQON"
- Strengthened the `sqon` param description (`executeQueryTool.ts`): added the fieldName-beside-op failure as a second WRONG example, plus "always use a group as the root, even for a single condition" (also sidesteps the known root-leaf `buildAggregations` crash)
- Strengthened the `execute-query` tool description with an explicit "translate the request into a SQON filter tree; call get-sqon-schema if unsure" step
- The cheat sheet is hand-maintained in `tools.ts`, not derived from the schema: smaller LLMs pattern-match examples rather than execute a recursive JSON Schema, and few-shot examples cannot be auto-generated

Open thread: awaiting a retest of the revised cheat sheet/descriptions against gemma-4-e4b; if prompt engineering alone isn't enough for a ~4B model, the higher-leverage fix is a mistake-aware validation error that lets the model self-correct in a loop.

Open thread: the `sqon` input is still `zod.unknown()` (no protocol-level structure for schema-constrained clients); Zod union errors still collapse to `Invalid SQON at root: Invalid input`, breaking the self-correction loop; the published JSON Schema still has dangling `$ref`s.
