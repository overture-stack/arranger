- `.dev/tech-debt.md` (apps/search-server): added an entry for the missing boot-time warning when a network node config omits `nodeId`, which silently degrades node-scoped chart filtering with no operator-facing signal.
