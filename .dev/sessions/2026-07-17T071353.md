Followed up on the non-obvious-defaults tech-debt item logged the previous session (found while reviewing an external OHCRN platform PR): corrected the misdocumented item, then built the consolidated reference doc that item's fix had proposed.

- `docs/migration/v3.1.md`: corrected the claim that `GRAPHQL_MAX_ALIASES`/`GRAPHQL_MAX_DEPTH` are "unset by default, meaning no limits apply". Confirmed in code (`queryValidation.ts:9,35`, wired in `graphqlRoutes.ts:217`) that both actually default to 15/7 when unset. Also reconciled the following paragraph's "conservative baseline" suggestion, which no longer made sense once the real defaults were stated.
- `docs/usage/07-defaults-and-limits.md`: new page, the single reference for every non-obvious default identified so far (`hits(first)`, the six aggregation defaults found in `createFieldAggregation.js`/`resolveAggregations.ts`, the download sort tie-breaker, and the two query validation limits), each with its default value and override mechanism. Cross-linked from `02-query-processing.md`'s pagination example.
- `.dev/tech-debt.md`: removed the "non-obvious behavioural defaults" entry now that its fix (the consolidated doc) exists; the misdocumented `GRAPHQL_MAX_ALIASES`/`GRAPHQL_MAX_DEPTH` item moved to "already documented" before removal, since the docs correction landed first.
- `.dev/roadmap.md`: added a sixth MCP-integration-readiness item ("Make invisible query defaults visible via SDL") capturing the residual, larger design question the tech-debt entry couldn't resolve on its own: whether these resolver-only defaults should become real SDL argument defaults for introspection visibility, separate from the documentation gap that's now closed.

The separately-logged `downloads.maxRows` enforcement bug (property-name mismatch between `getAllData.js` and `modules/types`) is unrelated to this doc work and is still open in tech-debt.
