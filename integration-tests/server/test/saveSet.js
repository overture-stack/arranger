import { expect } from 'chai';

export default ({ server, api, port, esHost, projectId }) => {
  it('should successfully save a set', done => {
    // TODO: write this test
    expect(true).to.equal(true);

    done();
  });
};
