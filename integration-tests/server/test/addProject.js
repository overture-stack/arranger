import { expect } from 'chai';

export default ({ server, api, port, esHost, projectId }) => {
  it('should successfully add a project', done => {
    /**
     * todo: write this test against the new admin
     */
    done();
  });
};
